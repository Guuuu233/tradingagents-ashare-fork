# AGENTS.md

本仓库的 AI 编码代理工作规范。**开始任何编辑之前先完整读一遍本文件。**

---

## 0. 铁律

违反以下任何一条，都视为任务失败，无论功能是否实现：

1. **读完再改。** 编辑任何文件前，必须先完整读该文件，并用 grep 找出所有调用它的地方。不允许只读片段就动手。
2. **修改原路径，不要新增并行路径。** 修 bug 就改原来那段代码。禁止新写一个函数绕过旧函数、禁止加 `_v2` / `_new` / `_fixed` 后缀、禁止用 if 分支把旧逻辑保留在旁边。
3. **删掉死代码。** 被替换的旧实现必须删除，不允许注释掉保留。git 会记住历史，不需要你在文件里留坟墓。
4. **一次提交一个关注点。** 数据清洗和网络重试是两件事，必须两个 commit。如果一次任务涉及多个关注点，分多次提交并说明。
5. **不要自行提交到主分支。** 完成后展示完整 diff 并说明改了什么、为什么。等我确认后再提交。

---

## 1. 会话纪律

- 一个会话只做一个任务。如果我在同一会话里提出了不相关的新需求，请提醒我另开会话，不要直接接手。
- 任务开始时，先用一段话复述你对任务的理解、你打算改哪些文件、以及你认为的风险点。**等我确认后再动手。**
- 任务结束时，明确说出：改了哪些文件、没改哪些本来可能相关的文件、以及你**不确定**的地方。

不要说"已彻底修复"。没有测试覆盖的修复不叫修复，叫推测。

---

## 2. 项目结构

| 目录 | 内容 | 注意 |
|---|---|---|
| `tradingagents/` | 多智能体核心逻辑 | 改动需格外谨慎，这是上游 fork 的部分 |
| `api/` | FastAPI 后端 | |
| `frontend/` | React + TypeScript 前端 | |
| `scheduler/` | 定时任务 | |
| `tests/` | 测试 | |

不要在这些目录之外创建新的顶层目录，除非先说明理由并获得确认。

---

## 3. 数据层规范

本项目大量依赖外部金融数据接口（akshare 等）。这些接口**极不稳定**，以下规范是从真实事故中总结的：

### 3.1 永远按列名取数，禁止按位置切片

```python
# 错误 —— 曾导致基本面分析师收到满屏 NaN
df.iloc[:, :18]

# 正确
WANTED = ["总资产", "总负债", "净资产", "归属于母公司所有者的净利润"]
cols = [c for c in WANTED if c in df.columns]
missing = [c for c in WANTED if c not in df.columns]
```

外部接口的列顺序、列数、列名随时会变。A 股财报接口有上百个字段，其中大量是金融机构专属科目，对普通企业全是空值。**按位置切片必然出事。**

### 3.2 缺列必须显式上报

取不到的字段要进 `missing` 列表并向上传递，不能静默丢弃。下游需要知道"这项数据没拿到"和"这项数据是零"的区别。

### 3.3 注入 LLM 之前必须清洗和压缩

不允许把原始 DataFrame、原始 JSON、原始表格直接塞进 prompt。注入内容必须是：

- 已剔除全空列与无关列
- 已转成紧凑文本（不是 markdown 大表格）
- 关键指标已在 Python 侧算好（比例、同比、分位），不要让模型自己在一堆数字里找

判断标准：**如果注入内容里出现连续的 `NaN`、空字符串或与本次分析无关的字段，就是没清洗干净。**

### 3.4 数据获取失败不得返回空值

```python
# 错误 —— 模型看到空白会自行编造
return ""
return None
return pd.DataFrame()

# 正确
return "【数据获取失败】限售解禁数据 — 原因：接口超时。该项不可用，不得据此判断无风险。"
```

### 3.5 禁止依赖外部数据源的隐式形状
外部接口返回的行序、列序、字段存在性、值的可解析性都不可信。
- 取"最新"必须显式 sort_values 后再取，禁止 .head() / .iloc[0]
  直接当作最新
- 日期比较必须转 datetime，禁止字符串比较
- 按日期查询的接口必须先规整到有效交易日，且只向前回溯
- **解析失败时禁止填充默认值**，尤其禁止填充"当前时间"。
  解析失败必须拒绝该条数据并记录日志。填默认值会把一个
  可检测的失败变成不可检测的错误答案。

本项目已因违反本条产生四次事故：
iloc[:,:18] 取错列、head(4) 取错行、curr_date 未规整交易日、
时间解析失败填今天。

---

## 4. 网络与并发规范

- 所有外部调用（LLM API、数据接口）必须有超时、重试、以及**明确区分的失败类型**（超时 / 认证失败 / 限流 / 返回结构异常）。
- 流式响应必须处理中途断连：收到不完整或空响应时，必须重试或明确报错，**绝不能把空字符串当作正常结果继续往下走**。
- 并发调用同一后端时必须限流。不同后端分别限流，不要用一把全局锁。
- 单个 agent 的失败不得中断整个任务流程。

---

## 5. 代码规范

- 禁止裸 `except:` 和 `except Exception: pass`。捕获要具体，且必须记录日志。
- 禁止静默失败。任何被吞掉的错误都必须留下可查的痕迹。
- 函数超过 60 行就拆。嵌套超过 3 层就重构。
- 魔法值必须提取为具名常量，尤其是字段名、阈值、超时秒数。
- 新增第三方依赖前先问我。能用标准库解决就不要引入依赖。
- 注释写"为什么"，不写"是什么"。`# 循环遍历列表` 这种注释直接删掉。
- 遵循仓库现有风格。新文件的组织方式要和同目录下已有文件一致。

---

## 6. 测试要求

任何 bug 修复必须附带一个**能复现该 bug 的测试**——先确认它在修复前失败，修复后通过。

任何新增的数据接口适配器必须有三个用例：正常返回、接口失败、返回结构异常。

不要写只断言 `assert result is not None` 的测试。断言具体内容。

---

## 7. 安全

- API Key、密钥、token 绝不能写入日志、返回给前端、或硬编码进源码。
- 不要在提交信息或注释里包含任何凭证。
- 涉及用户数据的接口必须校验归属，不能跨用户读取。

---

## 8. 提交规范

- 提交信息用中文或英文均可，但必须说明**为什么改**，不只是改了什么。
- 不要在提交信息里写"彻底修复""完美解决"这类形容词。
- 关注点拆分见 §0 铁律 4。

---

## 9. 当你不确定时

停下来问，不要猜。

以下情况必须先问再动手：
- 需要修改 `tradingagents/` 下的上游核心逻辑
- 需要改动数据库 schema
- 需要引入新依赖
- 发现任务描述与代码实际情况矛盾
- 修复方案有两种以上合理选择

**问一句的成本，远低于写出一堆需要回滚的代码。**

---

## 10. 跑真实分析：账户与端点

真实分析会消耗额度、写生产库、且结果可能进入 H1b 样本池。**用错账户的代价不是报错，是静默用到别人的配置。**

### 10.1 固定账户

| 项 | 值 |
|---|---|
| user_id | `429163f7-50b6-4982-8bdf-96ae99506843` |
| 邮箱 | `davidliu022305@gmail.com` |

**派工卡、脚本、命令里一律引用 user_id，不要引用邮箱。** UUID 抄错会立刻报错；邮箱抄错会静默命中别的账号。

⚠️ 库里存在形似邮箱 `davidliu022035@gmail.com`（`4f006165-…`，中间两位数字对调，2026-08-25 误注册，0 份报告）。它不是本人账号，**不得使用**。

### 10.2 禁止依赖默认回退

`user_llm_configs` 里有多个账号、`backend_url` 各不相同。代码在取不到账户配置时会回退到默认 base_url，**回退结果是别人的端点和 key，表现为 401**。

因此跑分析前必须实测回读一次：

```sh
sqlite3 "file:data/tradingagents.db?immutable=1" \
  "select backend_url from user_llm_configs where user_id='429163f7-50b6-4982-8bdf-96ae99506843';"
```

确认拿到的是预期端点再跑。**不要从日志或他人转述里抄 URL**——曾出现转录笔误把 `100.67.61.23` 写成 `100.67:61.23`，而库里原值是对的。

### 10.3 端点可达性

该账户端点位于 Tailscale 网段。启动服务时 `no_proxy` / `NO_PROXY` 必须覆盖 `100.64.0.0/10`，否则请求被代理拦截。这是持久性需求，不是一次性补丁（`.env` 的 `TA_BASE_URL` 与 `TA_MODELS_FETCH_ALLOWLIST` 均含该网段地址）。

### 10.4 记账

- 跑分析前后必须实测报告总数，**不得推算**：
  `select count(*) from reports;`
- 任务层返回成功 ≠ 报告已落库。验收必须贴出前后对照的实测值。
- 失败的分析可能完全不落库（失败在写库之前），此时计数不变属正常。
