# TradingAgents-AShare 增补整合施工计划 v1.1

日期：2026-09-06。状态：待用户／Cursor批准的施工计划，不是派工、提交、合入、部署或真实采集授权。

**目标：** 在保留已交付能力和原有未完成工作线的前提下，落实用户已确认的研究档位，完善证据贡献与命题审查，并建立可验证的收益评价条件。

**架构：** 沿用short/medium、现有claim/cluster/事件证据、四元状态和社交archive。在原路径补齐契约及消费链，不创建平行决策系统。数据读取、研究过程与收益评价分别验收。

**技术栈：** 现有Python 3.10、FastAPI、LangGraph、SQLite、React/TypeScript/Vitest；外置MediaCrawler独立环境。无需因本计划升级依赖。

本版保留v1，只修解析优先级、旧锁测、工作包粒度、H1b与新评价隔离。此轮核对指定基线代码，未重新查询实时主干/看板；§2现场为v1核实记录。首批建议仅H-01，修订计划获批也不自动授权部署。

## 1. 本计划与旧计划的关系

这是增补整合，不是全量覆盖。v2.1讨论稿保留为设计背景；本计划记录后续已批准的产品选择，局部取代其中相关“待确认”，不删除其余约束。

| 内容 | 处理方式 | 具体边界 |
|---|---|---|
| D-008/D-009/D-010、状态机、PIT约束、审批纪律 | 沿用 | 不因本计划降低门槛 |
| DAV-628 C-04评估、daily_basic接线、C-05已交付的部分事件与旁证能力、社交修复 | 沿用已交付成果 | 不重新开“首次交付”卡；C-05 PDF/财务正文抽取未交付，不视为整线收口，也不因此新增PDF卡 |
| C-04价格通道、C-09-3归一、Track B真实启用、H1b | 保留剩余工作 | 看板卡少不等于这些需求已完成 |
| 档位选择与语义传递、关系贡献、命题审查 | 增补 | 原路径改造，分关注点交付 |
| 文本扩档覆盖显式选择、非法值静默回退 | 局部替代 | 替换为下述输入来源及优先级契约，不保留两套冲突规则 |
| 分析师固定horizon等同本次研究期限 | 局部替代 | 拆清专业观察窗口与本次研究档，不盲目缩短历史数据 |
| 旧报告默认T+5或旧期限统计 | 兼容保留 | 标legacy，不悄悄解释成新的T+10/T+40 |
| Driver行业模板、统计相关权重、风险辩论删除、早期入场策略 | 研究保留项 | 不列入首批功能实施，不凭本计划确定参数 |

计划内I/H/E/D/V/B/R编号是本文件工作包编号，不是新建的Multica卡。实际派工前先检索已存在的任务并关联旧卡；每一关注点按独立实现、独立审核、Cursor放行执行。

## 2. 现场与证据边界

v1编制时只读核实的远端主干和本机healthz均为：

`c83881809da88686c30f097b1c3872187a5733ca`

healthz状态ok、executor_queued=0、executor_threads=4。记录不表示批准或调整并发。基线检查目录为 `/tmp/ta-run-c838818`。所有下列仓库路径均相对于此Git版本；实施时换成最新经批准的干净工作树，不能使用此临时路径假定永久环境。

DAV-665/666已done，DAV-667/668已cancelled；保留DAV-628评估。用户上轮报告仅DAV-648为todo，本轮没有据此推断所有未建卡需求已完成。未执行测试、实盘分析、采集、回填或部署。

H1b的121样本/79.34%来自宿主 `work/h1b_gates_report.json` 的2026-09-04未提交修改；基线Git版是2026-08-25、N=3。二者不可混称。执行H1b前须对明确数据库重新只读核算，不沿用陈旧数字放行。

现有具体落点：

- `api/main.py::_normalize_analysis_horizons`：双周期query会扩展单档显式列表，未知值被过滤；`_get_horizon_analysts`返回全部传入分析师。
- `tradingagents/graph/data_collector.py::get_window`：深拷贝池并写窗口元数据，本身不裁剪记录；`_fetch_all`采集长历史。
- `tradingagents/agents/analysts/`下market、news、smart_money、volume_price、social节点写死short，fundamentals、macro写死medium。这是实际档位联动的修复落点，而非要求删掉专业观察窗口。
- `api/services/calibration_service.py`消费现有报告概率和价格窗口；新期限不能直接套用旧默认值。

## 3. 已确认产品定义与待批准实施细节

### 3.1 用户已确认，不再重复问

| 设置 | 确认值 |
|---|---|
| 短期 | 5—20个交易日，主评价T+10 |
| 中期 | 21—60个交易日，主评价T+40 |
| 默认 | 短期，提交前可见，可选中期或双档 |
| 优先级 | 显式选档优先；文字不能暗中改变档位 |
| 双档 | 分别报告，不能平均成中性 |
| 产品范围 | 单股研究与交易建议；无账户数据不生成伪精确仓位 |
| 持有意图 | investment_horizon保留为背景，与horizons不合并 |

区间是产品定义，不代表已验证为最优参数。辅助观察日暂不新增；已有诊断点可保留legacy身份，但不能充当新主评价或新增独立样本。

### 3.2 本计划提出的实施契约，须随计划审定

1. 解析：显式合法列表→采用；显式包含任意非法值／空列表→验证错误；未提供字段→short且resolution_source=default；双档只能由显式合法列表选择。文本最多提示可改选，不改变resolved。显式null或空列表均报错，不与字段缺省混同。显式单档与文本冲突时保持显式档，返回非阻断提示。不把Pydantic默认值误认为用户显式选择。
2. 请求状态保留requested、resolved、resolution_source；新配置标识建议为 `horizon_profile_v1`。字段最终命名集中定义一次，复用现有horizon，不新增同义运行模式。
3. H链只声明已确认的主评价目标T+10/T+40及profile版本，不据此生成实际收益标签、已解析评价日或评价资格。
4. T的定义、该日收盘是否可作基准、现金分红总收益或价差、可执行入场时点，全部留在V-01审定。用户确认目标期限不等于这些口径已冻结。
5. H1b继续原T+5路径；不得改变calibration_service的DEFAULT_HOLD_DAYS=5或shadow_credit的T+5语义。新标签由horizon_profile_v1显式选择T+10/T+40，未知profile拒评，禁止缩短窗口。

H链可保存“计划评价步长”，但不得标记evaluation_eligible=true或把计划步长冒充已计算结果。V链须先通过口径和数据资格验证；无合格价格则保持待评/缺失，不用默认旧路径兜底。

## 4. 总体推进顺序与依赖

```text
I-00 现场/旧新契约核对
 ├─ H-01 期限解析 → H-02 跑次留痕 → H-03 入口 → H-04 分析目标联动 → H-05 双档验收
 ├─ D-01 RAW/dividend契约 → D-02 价格链路验证 ─┐
 ├─ D-03 资金规模归一 → D-04 下游消费         │
 ├─ E-01 证据关系 → E-02 贡献约束 → E-03 命题│
 │                                  → E-04 预期分栏
 └─ B-01 社交真实环境核查/授权 → 原Gate0→2→3→4

H-05 + D-02 → V-01 标签契约 → V-02 只读评价 → V-03 对照实验
E链的效果比较：机制可先测，收益结论依赖V链
H1b：独立核查与既有启用门禁，不作为全部离线工作的前置
```

首批建议仅H-01，干净工作树，不碰D/E/V/B，不部署。H-01同SHA审核及获准合入后，才启动H-02子包。D-01可作为以后另批的第二卡，但不在首批同时启动；随后推进其余H链和E链，避免首次把档位、证据算法、价格通道都改掉而无法归因。B链保持原授权闸。该顺序是排工建议，非允许多个Agent同时写同文件。

API主文件、collector、report_service、decision_status、provider均是串行集成热点。只读审核可以并行；涉及相同文件的实现从前一已验收主干后代开始，不并行改同树。每包应拆成“失败测试→最小实现→通过测试→差异检查”的小步；下面的测试为待执行验收，不预报通过数量。

## 5. 工作包：期限全链路（增补及局部替代）

### I-00 现场与兼容清单

目标：防止重复施工及错误基线。查最新远端SHA、healthz、工作树与脏文件；用Git树和任务历史核对DAV-628等交付。读取当前AGENTS.md、DECISIONS.md及v2.1，列旧请求调用者、缓存层、报告字段、定时入口。

交付：本计划基线差额与当前允许修改路径，不另写一份C-04评估。前置不满足或主干不是已审核线性后代，停止相关实现，不reset/clean宿主。

### H-01 期限配置与唯一解析

修改：`api/main.py`、`tradingagents/graph/intent_parser.py`；可新增唯一公共定义 `tradingagents/graph/horizon_profile.py`（拟新增，不是既有文件）。测试：新增 `tests/test_horizon_profile_contract.py`，必须替换 `tests/test_report_dual_horizon.py` 开头锁定旧规则的测试，并回归 `tests/test_dual_horizon_bugs.py`、`tests/test_intent_parser.py`。

步骤：先修改test_report_dual_horizon中“short+双档query扩档”和“unknown回short”的锁测，确认旧实现出现预期失败；补缺省/显式null/空列表/非法混合/双档去重及query冲突测例。直接修改原_normalize_analysis_horizons或迁移后删除旧实现，禁止两套解析并存，不要求无理由删除原函数。

点名检查api/main.py约2861和2885附近的两次归一化、_parse_intent以及聊天抽取到AnalyzeRequest的路径。用字段是否实际提供等机制保留原请求来源；intent_parser中写死的[short]和LLM抽取的horizons都不算用户显式列表。文本即使说“双档”也只能产生提示。二次调用不得将default翻成explicit或重写resolved。集中定义配置，不碰收益计算。

验收示例：显式medium+“短中都看看”仍只medium并有提示；未提供+明确双档文本仍为short/default，可提示选择双档；显式[short,bogus]报错，不静默留short；历史无版本请求可运行但来源标default/legacy，不宣称有显式选择。

风险：旧调用者依赖空列表回退。实施前点名调用者并更新内部调用；外部API返回清晰验证错误，不能全局隐式兼容非法新请求。

### H-02 跑次元数据与报告/缓存隔离

修改：`api/main.py`、`api/services/report_service.py`、`tradingagents/graph/propagation.py`、`tradingagents/graph/trading_graph.py`、`tradingagents/agents/utils/agent_states.py`。测试：`tests/test_report_dual_horizon.py`、`tests/test_report_social_context.py`，新增 `tests/test_horizon_run_metadata.py`。

步骤：先测单双周期最终、部分失败和读取回显；在现有result_data中保留请求/解析/配置版本/预测目标/cutoff/持有意图；更新各payload路径；盘点派生缓存键，加入真正影响结果的上下文；原始数据缓存仍可共享且不被裁剪污染。

验收：同股票日期不同档位和配置不串裁决；旧报告明确legacy/unknown，不回填新T+40；保留社交上下文，重试/恢复仍能读到原跑次配置。首选JSON元数据，不为此擅自迁移新DB列。

### H-03 前端与任务入口统一

修改：`frontend/src/services/api.ts`、`frontend/src/types/index.ts`、`frontend/src/components/ChatCopilotPanel.tsx`、`frontend/src/pages/Portfolio.tsx`、`api/services/scheduled_service.py`及`api/main.py`对应入口；如复用需要新增 `frontend/src/components/AnalysisHorizonSelector.tsx`，禁止保留两套含义冲突控件。新增对应 `AnalysisHorizonSelector.test.tsx`、`tests/test_horizon_entrypoints.py`；回归 `tests/test_watchlist_scheduled.py`、`tests/test_scheduled_queue.py`。

步骤：先测请求体而非只测按钮颜色；普通/聊天/定时/批量入口显式发送解析意图；默认短期可见；区分“计划持有”和“分析档位”；通过错误展示和冲突提示；单独确认各入口是否支持双档，不得把不支持入口静默当short，需补兼容或显式拒绝。

验收：用户无需在自然语言里写周期；选中期实际请求/报告为中期；双档分别显示；非法API请求与UI语义一致。宿主api.ts个人WIP不纳入提交，从干净基线按hunk实施，保护模型配置。

### H-04 研究期限与专业观察窗口解耦

修改：`tradingagents/graph/intent_parser.py::build_horizon_context`、`tradingagents/graph/data_collector.py::get_window`、`tradingagents/agents/analysts/{market,news,fundamentals,macro,smart_money,volume_price,social_media}_analyst.py`、`tradingagents/agents/managers/research_manager.py`；如需同步仅改相关 `tradingagents/prompts/zh.py`、`en.py`。新增 `tests/test_horizon_analyst_context.py`。

步骤：逐分析师测固定horizon覆盖运行horizon的问题；保留agent自身观察窗/指标预热历史，将本次研究档单独传入；让短期问题、中期传导、有效期限与失效条件进入各节点和总监；校正trace的horizon含义；共享池保持不变。

验收：市场节点可以在中期任务中使用短期价格证据，但trace不把该任务变成short；中期也读取新事件，短期不丢财务风险；不足历史标缺失，不制造MA/波动指标；两个档问题与行动依据不同，不要求结论必须不同。禁止只把14天替换为10天、90天替换为40天，持有期限不是回看天数。

### H-05 双档、失败与产品验收

修改：`api/main.py`、`tradingagents/agents/utils/decision_status.py`、`frontend/src/components/DualHorizonReportSection.tsx`、`frontend/src/utils/reportDualHorizon.ts`，仅在失败用例证明有缺口时修，不重写已通过的聚合机制。

测试：`tests/test_dual_horizon_e2e.py`、`tests/test_trading_graph_multi_horizon.py`、`tests/test_report_dual_horizon.py`及组件同名测试。

验收：valid/invalid三组合、short WAIT/medium BULL不被平均；两档均失败为无效运行；单档失败不参与校准。无账户资料只给研究观点、执行条件和风险限制，不给伪精确账户仓位。真实UI手验需有后续运行授权；离线API+组件测试不冒充实际部署验收。

### H链实施拆分与热点串行表（子包是交付单位，禁止整父包单提交）

| 子包 | 唯一关注点与允许热点 | 验收 |
|---|---|---|
| H-02a | agent_states、propagation：跑次元数据传递 | 请求来源与profile到state；不改payload |
| H-02b | trading_graph、api/main、report_service：元数据回显落库 | 单双档/读取留痕；不改缓存 |
| H-02c | 实际派生缓存落点（开卡前点名）：键隔离 | 换档/换配置不串结果；不裁原始池 |
| H-03a | api/main、scheduled_service：入口接线 | 缺省/显式来源不被中间层伪造 |
| H-03b | api.ts、types、选择器、ChatCopilotPanel：交互选档 | 请求体与界面一致 |
| H-03c | Portfolio及定时/批量测试 | 复用选择器，实际入口测试 |
| H-04a | intent_parser::build_horizon_context、必要state传递 | 专业窗口与研究档分别可见；不改分析师 |
| H-04b-1…7 | 每次仅一个分析师文件及其相关prompt小节 | 逐个验证运行档/观察窗/trace；七份独立差异 |
| H-04c | data_collector窗口契约（仅测试证实需要时改） | 元数据诚实、共享池不变，不实现PIT价格 |
| H-04d | research_manager：消费各节点研究档 | 期限对应核心问题/行动依据，不改计权算法 |
| H-05a | api/main、decision_status：仅修已复现的失败聚合缺口 | 三种有效性组合，不重写已通过逻辑 |
| H-05b | 双档前端组件/工具 | 不平均、失败档可见 |
| H-05c | 联合回归，仅测试 | 不顺带功能修复；失败另定位到所属子包 |

同一子包若产生独立逻辑改动仍须继续拆小。各子包继承父包列出的测试；H-04b按一个分析师一个测试选择器运行。上游schema可先添加非生效字段，消费修改独立交付，不能提前让未适配节点输出虚假的新语义。

热点次序：api/main按H-01→H-02b→H-03a→H-05a；collector按H-04c→D-02接线→D-03接线；decision_status按H-05a→E-03c。每次从前一获准合入的线性后代开工；没有修改需求的节点跳过，不制造空提交。D-02拆“口径映射→provider/collector接线→收益消费者验证”，D-03拆“纯归一计算→collector接线”，禁止把同文件不同线塞进一包。

## 6. 工作包：原有数据施工继续，不重开评估

### D-01 C-04-2 RAW与dividend读取契约（沿用）

依据：DAV-628 `work/2026-09-05-c04-pit-raw-dividend-eval.md`。修改：`tradingagents/dataflows/providers/cn_akshare_provider.py`；新增 `tests/test_tushare_raw_daily_contract.py`、`tests/test_tushare_dividend_contract.py`。

步骤：先以已验证接口字段和脱敏响应写日期/单位/空表/修订/跨证券负例；封装已有私有网关读取，不连接业务流；凭实际文档或授权探针核对schema，资料不足先停止相应接口实现。接口请求不等于字段已可用；不硬编码密钥、不直连替换用户网关。

验收：raw不得来自qfq默认回退；分红空表不是无分红；当前adj_factor不得宣称历史PIT可得。本计划不提供未经核验的外部参数模板；真实探针需独立说明授权和输出脱敏范围。

### D-02 C-04-3 价格业务链及归属口径（沿用）

修改：上述provider、`tradingagents/graph/data_collector.py`、`api/services/backtest_service.py`及对应报告入口；新增 `tests/test_price_basis_pipeline.py`，扩展 `tests/test_backtest_calibration_isolation.py`。

步骤：先核对backtest_service的vendor_qfq/unspecified与report_service的price_basis.unspecified等实际枚举并给出兼容映射测试，不直接宣称pit_raw已是统一值。再冻结原始价格、vendor_qfq、PIT序列的现有枚举映射，解决评估稿pit_raw等拟名与实际代码差异；接raw及公司行动旁证；证明报价/目标/止损/收益比较使用一致单位和基准；未来事件不能改变历史可得输入。真正PIT调整引擎不在此包暗中补出，无数据则禁称PIT。

验收：除权跨日、后续修订、供应商纠错、缺历史来源、未来因子、停牌均有负例。允许先交付明确raw通道，不以文档写了PIT作为收益链路通过依据。

### D-03 C-09-3 资金规模归一（沿用）

修改：`tradingagents/dataflows/fund_flow_evidence.py`、`tradingagents/graph/data_collector.py`，复用已交付daily_basic，不新增同名fetcher。新增 `tests/test_fund_flow_scale_metrics.py`，回归 `tests/test_tushare_daily_basic.py`。

步骤：先固定真实资金金额/市值/成交额单位与日期对应；计算分别具名的净额/流通市值、净额/成交额等比率；记录分母来源与缺口，零/负分母拒算；缺市值可保留成交额占比，但不能当同一量纲评分直接替代。

验收：同单位可复算、错单位拒绝或明确换算；跨交易日/证券拒绝；缺分母保留绝对额但禁跨股强度排名；不把多源一致度当影响大小。

### D-04 资金归一消费与旧语义回归（增补验收）

修改：`tradingagents/agents/analysts/smart_money_analyst.py`及实际消费的formatter，总监若需仅在独立提交修改。新增 `tests/test_fund_flow_scale_consumption.py`。

验收：归一信息进入报告且可追溯；reference_only不回退，双源冲突不择优删除；不能通过归一功能开启信用加权、推断机构身份或新增未经校准的分数。

## 7. 工作包：证据与命题（增补，不重建Ledger）

### E-01 证据关系最小结构

修改：`tradingagents/dataflows/news_event_evidence.py`、`tradingagents/agents/utils/claim_cluster.py`、`tradingagents/agents/utils/debate_utils.py`；新增 `tests/test_evidence_relations.py`。

步骤：列现有证据/claim字段与最小关系契约差额；为来源复述、同观测派生、支持/反驳/修订建可验证引用；拒绝断链/自循环；未知关系显式unknown，不让LLM随意宣称独立。

验收：canonical_event_id仅按现有主源契约引用，CollateralRecord仍None；claim ID不被事件ID替代；同一事件不同财务事实不强并。自动因果为hypothesis，不能冒充verified。

### E-02 贡献约束替代关键词单独决定

修改：`claim_cluster.py`、`research_manager.py`及必要的`evidence_verifier.py`；扩展 `tests/test_claim_cluster.py`，新增 `tests/test_evidence_duplicate_invariance.py`。

步骤：先写公告复制、语义改写、换Agent、同观测指标、真正独立验证的对照；基于E-01已判定关系做确定性去重和分组贡献约束；保留原始引用用于审计；输出为什么合并/不合并。

验收：已判定同事实的复制不增加有效支持；新的独立观察可保留增量。精确ID关系有硬断言，无法确定的语义复述用冻结fixture覆盖并披露覆盖率，不宣称能识别全部同义文本。禁止填0.8相关矩阵或新的默认因子权重。

### E-03 命题审查贯穿辩论与总监

修改：`tradingagents/agents/utils/debate_utils.py`、`evidence_verifier.py`、`decision_status.py`、`tradingagents/agents/researchers/bull_researcher.py`、`bear_researcher.py`、`tradingagents/agents/managers/research_manager.py`；新增 `tests/test_claim_review_contract.py`，复用确认闸相关现有测试。

步骤：复用现有claim及adopted/rejected状态，补证据引用、适用档、失效条件和状态变更理由；第一发言者不得引用不存在的对手命题；新取证需求走正常入口；命题假设未验证不得升级事实。

验收：已拒绝对手claim不全局阻断；采用命题中的PIT失败不能被总监洗白；仅观察假设不导致全单WAIT；原证据/命题/裁决变化可回溯。暂不删风险辩论，不固定辩论轮数或放宽3/1现有设置。

#### E-03子包（顺序交付，不作一次大改）

- E-03a：仅debate_utils与命题结构测试，补引用/适用档/失效条件和变更记录；不修改辩论或确认闸。
- E-03b-1/2：分别适配bull_researcher、bear_researcher及测试；第一发言者不可虚构对手，保持现有轮次。
- E-03c：evidence_verifier与decision_status消费，须在H-05a之后；先测采用/拒绝、PIT失败及观察假设，不碰总监prompt。
- E-03d：research_manager消费及联合回归，须在H-04d和E-02之后；保证结构化对象真正影响审查且状态留痕。

各子包独立同SHA审核；tests/test_claim_review_contract.py按子包逐步增加，不提前宣称未实现行为通过。

### E-04 基本面/事件/预期修正分栏

修改：`news_analyst.py`、`fundamentals_analyst.py`、`research_manager.py`及相关prompt；新增 `tests/test_expectation_revision_contract.py`。

步骤：从合格正文事实与既有财务记录形成分栏；旧基线明确属于管理层/一致预期/自身旧预测/隐含模型；无基线仅定性；区分新发布、新内容、超预期、是否已定价。

验收：标题或PDF字节hash不能变成已读毛利率；没有旧基线不能生成修正幅度；forecast不是实际半年报；同一影响已计入预测不重复加票。可靠正文抽取若仍缺失，作为数据缺口保留，不能用该包暗中实现通用PDF解析器。

## 8. 真实采集、评价和研究保留线

### B-01 Track B AUTH（沿用DAV-648，不重复建父卡）

依据 `docs/social_data/acceptance_checklist.md` 和原实施方案。现有代码修复不再重复做。先只读核实真实MediaCrawler目录/版本、任务、源库、归档与安全登录条件；需要安装、登录、采集、导入、切模式、重启时集中申请对应授权。

按原Gate真实双平台首轮→归档不可变验证→30报告/10股shadow→2—5股canary→全量决定。报告能关联合格快照才算闭环；disabled不证明外部采集停止。历史无快照不能事后回填。原清单新增等待时长和角色称谓若需执行，先确认其授权依据，不能靠文档自授生产权限。

### V-01 收益标签契约（依赖H-05、D-02）

修改：`api/services/backtest_service.py`、`api/services/calibration_service.py`，复用 `tradingagents/dataflows/trade_calendar.py`；新增 `tests/test_horizon_return_labels.py`。

本包必须先产出经审定的标签契约，再实施评价；现有_get_price_after从base_date+1取排序后第hold_days条记录，不能认定等价于交易日历T+N（停牌/缺行可能不同）。先用节假日、缺行及停牌负例证明差异。冻结§3.2尚未确定的评价口径、交易日T定义、cutoff资格、主评价日、研究基准与可执行入场的差别；给节假日、周末、缺目标日、公司行动和跨档旧报告写反例。未成熟样本标pending，不缩短40日。价格缺失标missing，不当0收益。定义尚不明确的统计不得发布。

### V-02 分期限只读评价及校准隔离

测试 `tests/test_calibration_service.py`、`tests/test_backtest_calibration_isolation.py`及新增标签测试。统计按期限/配置/模型/提示词版本分组，兼容旧样本但默认不混池；无效运行、失效档和不满足评价资格的记录排除并统计原因。WAIT的方向诊断与真实交易绩效分别列，避免选择性只评已交易样本。

在既有服务内部按明确profile路由，不另建平行校准服务。缺省旧调用仍保持原T+5；新profile显式T+10/T+40。为DEFAULT_HOLD_DAYS=5和shadow_credit的T+5分别加不变性回归，不修改H1b回填、门禁和旧cohort。禁止confidence转probability、把T+5旧结果当T+40；无组合规则不报组合Sharpe/回撤。只读分析不得顺便迁移或回填生产库；若既有服务隐式写库，改在隔离样本库验证并说明。

### V-03 机制与收益对照

现有冻结版、增量版、简单基准在同cutoff数据与明确交易假设下比较。先做复制/顺序/缺口/命题消融；收益结果待V-02合格后按时间外推，处理同事件泄漏、重叠标签和交易成本。原歌尔、工业富联、蓝思、美的、隆基/爱尔为回归集，不当未见样本。蓝思6月案例来源不全则列待补，不用事后走势强制BUY。

### R-01 H1b及远期研究（保留，不扩大门禁）

H1b用明确数据库、当前代码和只读输出核算；门槛未过保持KEEP_FALSE，不通过调样本、缩短持有窗口或降低比例门槛过关。本计划不授权回填、开启加权或重启。

Driver行业试点、统计相关、情绪极端逆向、风险辩论去留和早期入场另有实验协议。允许隔离研究，不必须等待H1b；禁止在没有验证时加入运行权重。行业、具体策略阈值仍待决定。

## 9. 测试命令、审核与发布分离

首批H-01专用（新增文件写好后）：

```bash
env -u PYTHONPATH /Users/davidliu/Documents/TradingAgents-AShare/.venv310/bin/python -m pytest tests/test_horizon_profile_contract.py tests/test_report_dual_horizon.py tests/test_dual_horizon_bugs.py tests/test_intent_parser.py -q
```

下面是未来父包完成后的汇总命令，不要求H-01创建或运行后续包测试。

实施者先在自己干净工作树确认测试文件存在；本计划标“新增”的测试需随对应实现创建，下面命令执行于实施树根目录，不在脏宿主运行功能测试。解释器沿用已配置3.10，不安装/升级包凑通过。

```bash
env -u PYTHONPATH /Users/davidliu/Documents/TradingAgents-AShare/.venv310/bin/python -m pytest tests/test_horizon_profile_contract.py tests/test_horizon_run_metadata.py tests/test_horizon_entrypoints.py tests/test_horizon_analyst_context.py -q
env -u PYTHONPATH /Users/davidliu/Documents/TradingAgents-AShare/.venv310/bin/python -m pytest tests/test_dual_horizon_e2e.py tests/test_trading_graph_multi_horizon.py tests/test_report_dual_horizon.py tests/test_report_social_context.py -q
env -u PYTHONPATH /Users/davidliu/Documents/TradingAgents-AShare/.venv310/bin/python -m pytest tests/test_evidence_relations.py tests/test_claim_cluster.py tests/test_evidence_duplicate_invariance.py tests/test_claim_review_contract.py tests/test_expectation_revision_contract.py -q
env -u PYTHONPATH /Users/davidliu/Documents/TradingAgents-AShare/.venv310/bin/python -m pytest tests/test_price_basis_pipeline.py tests/test_fund_flow_scale_metrics.py tests/test_fund_flow_scale_consumption.py tests/test_horizon_return_labels.py tests/test_backtest_calibration_isolation.py tests/test_calibration_service.py -q
git diff --check
```

前端在 `frontend/` 使用现有脚本：`npm test -- src/components/AnalysisHorizonSelector.test.tsx src/components/DualHorizonReportSection.test.tsx`，再`npm run build`。每包另外运行其明确列出的现有/新增测试。所有外部响应在机制测试中mock，必要时清除代理变量；不启动真实采集或LLM以证明单测。

预期：针对性旧缺陷测试先红、修复后绿；不得把不存在测试或错误导入当作有效红灯。交付写精确命令、解释器、完整SHA、真实通过/失败/跳过数及未测范围。全仓有已知失败须对照基线，不夸称全仓通过。

审批链保持：功能候选→同SHA独立审核→Cursor复核→明确准予合入→线性FF→远端回读。部署另批；部署后PID/cwd/DB/SHA与用户入口实际验收另证。不可将审核PASS当FF授权，也不可将FF当部署授权。

回滚以关闭新行为或回退经过批准的代码版本为单位，不删除archive、不重写旧报告、不恢复错误PIT口径。兼容字段不可用则暴露缺失；回滚不得静默把medium变short或把无效变HOLD。

## 10. 完成标准与本轮交付边界

H链完成：用户可显式选档、选择真的贯穿研究/报告，失败隔离与旧版本兼容有证据；不等于预测更准确。

D链完成：读取与消费契约有来源和单位/PIT验证；不等于所有历史可回测。E链完成：复制不加权、独立证据不误删、命题状态可追踪；不等于语义去重100%。B线完成：真实Gate证据闭环；V线完成：数据资格、标签和样本外评价分别成立；H1b仍按原门槛单独判定。

本轮只新增这份计划文档，未改v2.1原稿、代码、数据库、账号、模式、看板或任务指派。用户已确认产品定义，不代表每个候选实现、实验执行和真实启用已获批准。下一步由用户／Cursor审定此整合计划，再按实际卡片与新主干差额排工。
