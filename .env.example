# --- 核心模型接入 (Core LLM) ---
# 建议统一使用 TA_ 前缀，同时也兼容 OPENAI_API_KEY / OPENAI_BASE_URL
TA_API_KEY=
TA_BASE_URL=https://api.openai.com/v1
TA_LLM_PROVIDER=openai

# 模型名配置 (默认已提供常用值)
TA_LLM_QUICK=gpt-4o-mini
TA_LLM_DEEP=gpt-4o

# LLM 瞬时错误重试次数（0 完全禁用；默认 2）。
# 仅对"请求根本没有成功响应"的瞬时故障重试（网络抖动/EOF/上游 5xx/限流 429），
# 认证类(401/403)、参数类(400)、模型不存在(404)等不重试；正常完成的调用零重试。
# 可用构造参数 max_retries 覆盖。
# TA_LLM_RETRY_TIMES=2

# --- 安全密钥 (Security) ---
# 用于加密用户 API Key 和签发 JWT，生产环境必须设置！
# 生成方式: openssl rand -base64 32
# 首次设置时，系统会自动将已有数据从默认密钥迁移到新密钥。
# 未设置时服务（API 与调度器）拒绝启动。
# 仅限本地开发：如确需放行内置默认密钥，可显式设置 TA_ALLOW_DEFAULT_SECRET=1（不安全，禁止用于生产）。
# TA_APP_SECRET_KEY=

# --- 进阶可选配置 (Advanced Optional) ---
# 提示词语言 (auto / zh / en)，默认 zh
# TA_LANGUAGE=zh

# 运行日志追踪 (1 开启 / 0 关闭)，默认 1
# TA_TRACE=1

# 辩论轮数 (1-5)，默认 2
# TA_MAX_DEBATE=2
# 风险讨论轮数 (1-5)，默认 1
# TA_MAX_RISK=1

# 长任务软提醒与硬终止上限（秒）；0 表示禁用对应期限
# 默认运行 30 分钟后提示仍在继续，最长 2 小时后终止以释放调度资源
# TA_JOB_TIMEOUT=1800
# TA_JOB_HARD_TIMEOUT=7200

# --- 调度器优雅关停与任务持久化 (Scheduler graceful shutdown) ---
# 收到 SIGTERM/SIGINT 后等待进行中任务完成的最大秒数；超时则取消剩余任务，
# 被取消的任务保持 "running" 落库，下次启动会自动重新入队（不丢失当日分析）。
# SCHEDULER_GRACEFUL_SHUTDOWN_SECONDS=900
# 运行中任务心跳落库间隔（秒），用于启动恢复时区分"分析中崩溃"与"已放弃"。
# SCHEDULER_HEARTBEAT_SECONDS=30
# 启动恢复时的心跳过时阈值（秒）：last_job_heartbeat_at 距今超过该值视为
# "已放弃"置 stale（不重新入队）；心跳新鲜则视为"分析中崩溃"重新入队。
# 需大于心跳间隔数倍，慢分析或长维护窗口可调大。
# SCHEDULER_HEARTBEAT_STALE_AFTER_SECONDS=900

# --- 数据源 (Data sources) ---
# 可选：Tushare Pro 资金流结构化接口（moneyflow_dc / moneyflow_ths）。
# 未配置时资金流链路 fail-closed，并保留 legacy Web 参考标记。
# TUSHARE_TOKEN=
# 同花顺金融数据 API（fuyao.aicubes.cn）：财务数据主源 + 涨跌停/龙虎榜备用源。
# Key 请到 https://fuyao.aicubes.cn/ 登录后签发；不配置则 cn_fuyao 数据源不可用。
FUYAO_API_KEY=
# 可选：自定义同花顺 API 地址（默认 https://fuyao.aicubes.cn）
# FUYAO_BASE_URL=https://fuyao.aicubes.cn

# --- VLM 截图识别 (Vision Language Model) ---
# 用于持仓截图识别，默认使用智谱 glm-4.6v-flash（免费）
# TA_VLM_API_KEY=你的智谱API Key
# TA_VLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# TA_VLM_MODEL=glm-4.6v-flash

# --- 邮件服务 (Email/SMTP) ---
# 仅在需要开启邮箱验证码登录时配置
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=465
# MAIL_USER=你的邮箱
# MAIL_PASS=你的授权码
# MAIL_FROM=发件人名称
# MAIL_SSL=1

# --- 社交舆情数据 (Social sentiment data, MediaCrawler / Archive) ---
# 运行模式: disabled (默认关闭) / shadow (静默比对) / active (启用社交观点与热度)
# TA_SOCIAL_MODE=disabled
# 社交数据 Provider (默认 archive_sqlite)
# TA_SOCIAL_PROVIDER=archive_sqlite
# 社交归档 SQLite 数据库绝对路径 (shadow/active 模式必须配置绝对路径，不可为空)
# TA_SOCIAL_ARCHIVE_DB=
# 启用的社交平台列表 (逗号分隔，默认 xhs,dy)
# TA_SOCIAL_PLATFORMS=xhs,dy
# 回溯天数 (默认 7)
# TA_SOCIAL_LOOKBACK_DAYS=7
# 最大抓取帖子数 / 评论数 (默认 100 / 300)
# TA_SOCIAL_MAX_POSTS=100
# TA_SOCIAL_MAX_COMMENTS=300
# 最低可判定阈值 (默认 帖子>=3, 分类>=20, 作者>=10)
# TA_SOCIAL_MIN_POSTS=3
# TA_SOCIAL_MIN_CLASSIFIED=20
# TA_SOCIAL_MIN_AUTHORS=10
# 报告包含的证据样本条数上限 (默认 20)
# TA_SOCIAL_EVIDENCE_LIMIT=20
# 金丝雀测试股票代码白名单 (逗号分隔，为空表示对全部标的生效；active 模式下未命中白名单回落)
# TA_SOCIAL_CANARY_SYMBOLS=
# 归档读取独立超时时间 (秒，默认 5)
# TA_SOCIAL_FETCH_TIMEOUT=5

# 兼容说明: 系统自动识别 TA_ 前缀变量。
# 若需修改默认行为，取消上述注释并填值即可。
