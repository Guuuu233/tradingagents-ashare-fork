## ⚠️ 本卡合入结果存在阻断级缺陷，已开窄返修 DAV-998

本卡候选 `75e3c359` 虽通过 DAV-994 复审与完整 RT-FULL 对照（零新增失败）合入为 `5a0320f`，但事后经 Codex 只读审计发现并由运维独立复现：**所有持久化 typed refusal 都被当作永久终态，从回填队列中排除**（`historical_cases.py:793-799`），导致历史案例 T+1 学习闭环被破坏。

运维复现（`.venv310`，隔离 sqlite）：

```
① 落库案例: 成功   actual_outcome='【数据缺失】'   持久化 refusal = calendar_unavailable
② as_of = 今日+30天 回填: {'total_scanned': 0, 'backfilled': 0, 'still_missing': 0, ...}
```

`future_eval_date`、`eval_date_not_closed`、`calendar_unavailable` 三类**本质临时**的状态都会被永久排除，而「今天出报告、T+1 之后回填收益」恰恰是正常业务流程——即几乎每条新案例都会中招，收益永远停留在 `【数据缺失】`。

**为什么门禁没拦住**：现有测试与 RT-FULL 对照都只验证「单次调用的拒绝语义正确」，没有覆盖「拒绝之后、条件满足时能否被重新扫描」这一跨时间维度的行为。这是测试覆盖盲区，不是流程执行问题。DAV-998 的 AC-2 已针对性补上。

### 处置

- 主线**保留**该提交，采用前向修复（服务已停机、未部署，无线上影响）。
- **在 DAV-998 合入前，主线携带已知阻断级缺陷，不得部署**。
- 返修见 **DAV-998**（窄范围：只做终态 / 可重试的区分）。

本卡维持 done（合入事实成立），缺陷跟踪转入 DAV-998。
