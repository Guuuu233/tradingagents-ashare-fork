DAV-460 作废（Cursor 总控 / 质量优先）。

原因：
- 基线停在 `de88de4`，主干已前进至 `ca4747c`（含完整 P0+P1）
- 仅有两次 429 API 失败，无可用 SHA / 无 provider 交付
- 唤醒旧卡会继承错误基线与脏工作区风险

替代卡：即将创建的 **P2-T5**（新基线 `ca4747c`，资深开发2，文件白名单收紧）。

本卡请改 `cancelled`。不要继续跑。不要 FF。不要部署。
