# 当前剩余工作（2026-09-18，DAV-1055 合入后刷新）

本文件是当前运行态盘点，不替代历史方案和决定文档。

## 已完成并核实

- 主线已部署到 `4b540b0c9b08d77a12ce7d08cfdf0095288cd350`；8000 当前 PID `68658`（PPID=1），worktree `/private/tmp/ta-serve-4b540b0`，`/healthz` 精确回读同一 SHA。`/v1/social-data/status` 回读 `shadow/operational`，xhs/dy 均 operational，`analysis_availability.available=true`，归档库 `quick_check=ok`。生产库当前 `795 completed / 624 failed`；DAV-998 pending 仍为 `600519.SH` eval_date=2026-09-18，未提前填充。
- `DAV-1040` 已修掉行尾空格并由 `代码审核员` 对 `01cdd7de043b8a58cdf9e2ee62faa212d4b87b37` 同 SHA 复审通过；已与 `cdd201db` 合并为 `e30f312`。
- `DAV-1048` 已由 `资深开发2` 实施、`代码审核员` 对同 SHA `83f011e05548c0da3140623875ec4cc8d09b88bd` 复审 PASS，并合入为 `6ee1486`；启动回填重复扫描实测未再改变待回填记录的 `updated_at`。
- H1b 修复候选 `4b540b0` 已合入并部署；同 SHA 审查 PASS、定向 112 passed、组合全量与基线失败集合一致。受控真实 v2 分析 `5fd6de0b…` 完整成功但为 ABSTAIN，不增加合格样本；门槛仍 FAIL/KEEP_FALSE。
- `/` 根路径返回 200，前端 dist 已复制至当前服务目录；`/v1/reports` 返回 200，伪造发布探针返回 404。
- 生产库符号清洗、博弈论生产证据、社交 shadow 采集/归档均已保留原证据。

## 仍未完成，但受数据条件约束

1. V-03：只读基线和 7 变体消融证据已入档。DAV-1044 最新只读复核确认 `trade_date >= 2026-09-09` 有 2 条 completed、已持久化实际值 1 条、pending 1 条；不能当最终收益结论。证据见 `work/v03-forward-recheck-20260918-summary.md`。
2. V-03 离线护栏：代码已合入主线 `061f007`，并由 `代码审核员` 对同一 SHA 复审通过。它只改离线评估脚本、评估模块和测试，不被 8000 API 导入；线上当前运行 `4b540b0`，无需为离线评估单独重启服务。正式 V-03 证据应直接用 `061f007` 的评估器，在生产库只读副本上配合 `--offline --price-snapshot` 运行；当前缺的是合格的本地 T+5 价格快照和足够的有效前向样本。

## 仍需观察或授权

- DAV-998 当前 pending 案例须等评估日真正收盘后再核验实际回填；不得提前写结果。
- H1b 样本积累和启用涉及生产写入/功能开关，另行授权。
- 社交真实采集需要 Cookie、沙箱和 Gate0–3 授权，冻结不变。

## 当前施工顺序

1. 收盘后核验 DAV-998 的 `600519.SH` 是否真实回填；“机制可重试”与“实际回填成功”分开记账。
2. 具备本地 T+5 价格快照后，直接用 `061f007` 在生产库只读副本上做 V-03 FORWARD_OOS；不需要先部署 8000，也不把离线评估结果冒充线上运行态证据。
3. V-03 等真实前向样本达到总工明确的阈值后由 DAV-1044 做正式 FORWARD_OOS；当前基线不是收益结论。
4. H1b、社交 Gate0–3、Cookie/真实采集、生产库继续按冻结边界处理，不因本次合入自动解冻。

## 本次只读复核补充

- H1b 复核使用生产库 `.backup()` 副本；生产库备份前后文件 SHA 为 `e884d3ceb6b44e4af712f64dd17d5387a966ec4f7d3b190ba9c1f3d26dc47518`，`quick_check=ok`，逻辑计数仍为 `1410 / 794 completed / 616 failed`。
- DAV-1044 的 V-03 前向复跑已完成只读核验；09-17 案例仍未收盘，不得把它当成已评估样本。
- DAV-1044 已完成只读交付并转 `in_review`；DAV-1050/DAV-1054 已交付，DAV-1053 已记录为打回，DAV-1055 已由 `代码审核员` 对 `061f007` 完成同 SHA PASS。

## 当前看板快照

- 工程待办：0；运行中：0。
- `DAV-1044`：`in_review`，等待更多有效前向样本和本地 T+5 价格快照，不重复派工。
- `DAV-990`：历史诊断卡 `blocked`，不是当前发布阻塞，不因看板为空重复启动。
- 离线 V-03 护栏 `061f007` 已合入；线上当前运行 `4b540b0`，离线评估器不被 8000 API 导入，二者无需因评估任务再次重启服务。
