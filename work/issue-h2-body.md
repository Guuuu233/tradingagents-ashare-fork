## 目标
测试体系体检：全量回归基线、修复 2 个日期炸弹测试、审查 skip 原因、分析覆盖缺口、抽查测试质量。

## 背景
- 主干：`codex/dav-4-p2a-trunk` HEAD `3c339c5`（M1~M5 已竣工）
- 规划文档：`work/2026-08-05-code-audit-plan.md`（必读）
- **前置依赖：H1 审计完成并验收**（审计报告 `work/code-audit-report.md` 中与测试相关的条目由本阶段处理）
- 施工纪律：见规划第 4 节

## 任务清单
1. 全量回归基线：`env -u PYTHONPATH .venv310/bin/python -m pytest tests/` 记录通过/跳过/失败数
2. **修复 2 个日期炸弹**（`tests/test_vendor_chain_semantics.py` 两例硬编码 `2026-08-04`，上海时区过零点后失败）：改为动态取「今日」的日期相对写法。TDD：先写失败复现，再修复
3. 审查所有 skip 用例：原因是否合理？不该 skip 的恢复（如环境依赖类 skip 需说明）
4. 覆盖缺口分析：核心路径（数据获取、裁决链、任务持久化、校准统计）哪些没有测试，登记进审计报告
5. 测试质量抽查：断言是否有效、是否测实现细节而非行为

## 验收标准
- 日期炸弹修复有独立 commit（含复现测试）
- skip 清单审查结论明确（保留/恢复/说明原因）
- 覆盖缺口登记到审计报告
- 全量回归结果：修复后 0 新增失败
- 完成评论 @项目调度助手，等待 Hermes 验收后放行 H3

## 环境铁律
- 所有 Python 命令必须 `env -u PYTHONPATH`；测试用 `.venv310/bin/python -m pytest`
- 一个 commit 一个关注点；不自行提交主干
