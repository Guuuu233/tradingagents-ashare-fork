# V-03 当前版本只读实验任务

## 基线

- 固定代码基线：`df7538413ba7bb55593b1757feaf90b0bd514d1c`
- 这是只读实验，不得部署，不得启动真实分析，不得写入生产库。
- 使用项目记录的 Python 3.10.20 解释器，并清理 `PYTHONPATH`。

## 任务

在生产库的 SQLite 只读副本上运行 V-03 当前版本实验，至少覆盖：

1. 当前默认配置的收益测量；
2. 已有脚本支持的消融/对照项；
3. 反泄漏、交易成本、前向 OOS 截止口径和样本排除原因的核对。

优先复用 `scripts/run_v03_return_measure.py`，先确认它通过 `.backup()` 或等价方式只读复制数据库；任何无法保证只读的步骤立即停止并报告，不自行改用生产库写入。

## 硬边界

- 不改 `credit_weighting_enabled`，不积累 H1b 样本。
- 不使用社交 Cookie，不启动真实社交采集。
- 不写 `data/tradingagents.db`，不执行 API 真实分析。
- 不把历史的“进度基线”写成最终收益结论。
- 外部行情供应商若不可达，记录确切缺口和已完成的只读部分，不伪造结果。

## 交付证据

交付报告必须写明：代码 SHA、实际解释器、输入库备份前后 SHA/计数/`quick_check`、实验配置、每个对照的样本数与排除数、收益字段和成本口径、前视检查结果、原始日志路径。所有新文件使用唯一前缀 `work/v03-df753841-20260918-`。

完成后不要合入或部署；把候选 SHA（若有代码变更应为无）和报告路径发给项目调度助手，由总工决定下一步。
