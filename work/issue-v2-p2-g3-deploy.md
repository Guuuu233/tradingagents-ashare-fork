# P2-G3 部署

等 `target/codex/dav-4-p2a-trunk` 精确等于 `50679db31a7fa1908f5ba91d54aa7c39711605a0`。若仍是 `f8f624241d12…` → BLOCK（那只是合入前旧主干）。

项目根 `/Users/davidliu/Documents/TradingAgents-AShare`。在途报告 0。禁止 hard reset。FF 宿主到该 SHA。DATABASE_URL=`sqlite:///./data/tradingagents.db`，`.venv310`，代理 + 完整 no_proxy，重启 uvicorn。`/healthz.commit_sha` 必须等于 `50679db31a7f…`。3/1 不变。禁止改模型绑定。

不要 mention 项目调度助手。
