# 项目联合审计结论（2026-09-10，Codex + 总控双验）

对照《整合施工计划 v1.1》§4–10。结论：**项目未完成、不可发布**；主干推进到 `1b9bb44`，但生产接入/E链消费/真实社交/完整V-03/H1b 均未闭环。计划 §10 本身声明每链"完成 ≠ 成品"。

## 已确认缺口（真 trunk 1b9bb44 核实）

1. **E-02 只造未接**：`reduce_evidence_claims`（claim_cluster.py:591）存在且有测试，但**生产路径零调用**；`research_manager` 仍走 `tally_cluster_votes`（关键词聚类，line 382/440）。计划 §7 要求 E-02 进入 research_manager → 实际接线缺口。`evidence_relations.py` 除测试外无消费者。
2. **V-03a ≠ V-03**：现仅"单账号+completed 历史收益基线"。计划 §8 要求的四项在 `v03_return_measure.py` 中**全缺**：
   - 冻结版/增量版/简单基准 三方**机制对照**；
   - 复制/顺序/缺口/命题 **消融**；
   - **回归股集**（歌尔/工业富联/蓝思/美的/隆基/爱尔）——仅碰蓝思；
   - **重叠标签 purge/embargo**（此项曾在冻结决策中定过，未落实）。
3. **H/D 部分完成**：契约与消费代码入 trunk，但 raw 非 PIT、真实供应商/停牌/除权线上探针未做；UI/用户入口验收未做。
4. **B-01 真实 Gate 0–4 未做**：social 保持 disabled，未闭环。
5. **H1b FAIL，须 KEEP_FALSE**：门槛未过，不得开加权/补生产样本/改库。
6. **V-03a 报告 provenance 不自洽**：盖章 code=5a00c753、service=a6d4540，却位于 1b9bb44；需以一致 SHA/model/prompt/服务重生成。
7. **测试**：真全量 `18 failed / 3967 passed`（失败=基线既有，含语义漂移测试 + `cn_akshare.get_cninfo_announcement_content` 缺日期参数的真契约缺口）。

## 记录：DAV-804 血缘与总控前次误报（已纠正）

- 真链：`5a00c753 → 257199eb(DAV-802，被否候选) → 1b9bb44(DAV-804)`。被否的 802 commit **在 trunk 历史中**（是 804 第一父）。
- 总控合入时误称"第一父=5a00c753"（实为 257199eb），且未察觉 802 随之入史。**FF 仍合法**（1b9bb44 是 5a00c753 线性后代），**最终树干净**（804 的正确报告覆盖了 802 的污染报告，已核）。验收对象 = `1b9bb44`。

## 施工纪律（不变）

不改代码/库/看板/服务（审计轮）。进入施工：最小下一刀 = **E-02 接线 + 生产路径测试**（独立、低风险）；**完整 V-03** 须先按计划冻结实验协议再实施。H1b 维持 KEEP_FALSE。
