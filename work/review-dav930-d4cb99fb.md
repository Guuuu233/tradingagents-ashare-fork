# DAV-930 候选 `d4cb99fb` 只读代码复审

请由 **代码审核员** 对下面这个完整候选 SHA 做同一版本的只读复审。审查不修改、不合入、不部署；不要使用“独立代码审核员”。

## 版本边界

- 实施卡：DAV-930（热门股票旧接口保留缺失行情语义）
- 候选完整 SHA：`d4cb99fb7a230595d76f8b52e3101b871b20573c`
- 直接父 SHA：`b95a9b88c81e87a4da121f9945aedbe844fa04e0`
- 远端分支：`origin/agent/1/4a6078d30d63`
- 目标主线：`origin/codex/dav-4-p2a-trunk`，交付时 HEAD 为上述直接父

## 严格白名单

1. `api/main.py`
2. `tests/test_hot_stocks_api_contract.py`

越出白名单、父提交不一致、候选 SHA 不一致或检出工作树非 clean，直接打回。

## 复审重点

- 供应商缺失价格/涨跌字段时，`/v1/market/hot-stocks` 不得再把缺失值填成 `0`；必须以显式缺失语义（null / 缺口标记）返回，且不得把“没拿到”表述为“零变化”。
- 合法的真实 `0`（如真实零涨跌、停牌价 0 场景）必须与“缺失”严格区分，不得一并改成 null。
- 核对响应模型/序列化与既有前端消费者的兼容性：字段类型变化是否会让消费端把 null 再次渲染成 0 或 NaN。
- 确认该端点仍在对外暴露的前提下，其错误/空结果路径未被静默吞掉。

## 红队场景（须逐条实跑并贴输出）

- RT-1 供应商返回缺 price/pct 字段 → 响应为显式缺失，不出现 0。
- RT-2 供应商返回真实 0 值 → 保留 0，不误判为缺失。
- RT-3 供应商整体失败 → 明确失败/缺口，不返回伪装成功的空列表。
- RT-4 字段类型变更后的响应 schema 校验通过。

## 须独立复核的证据

- 固定 Python 3.10 环境，`env -u PYTHONPATH`。
- 精确 collected / passed / failed / skipped / deselected 数字。
- 因本刀改动产品代码（`api/main.py`），须按 D-012 §4b 提供 RT-FULL 全量回归，并与线上基线失败集合逐项对照；有任何新增失败即不得 PASS。
- 全程离线 mock，不请求真实供应商、不调用模型、不写数据库、不启动或重启服务。

请只读检出并复测完整 SHA，给出路径/行号、精确测试数字和明确评级（PASS / FAIL）。
