# Handoff — D-009 P0-1 施工进度（新窗口入口）

日期：2026-08-27 ~23:30（UTC+8）  
分支：`codex/dav-4-p2a-trunk`  
HEAD（已推 tip，**不含**本卡 WIP）：`de88de4eb33b7595d6fcb9a4c4e84d0a80967db5`  
状态：**P0-1 本地 WIP，David 尚未批准提交**（独立审查两轮均未放行；第 2 轮 5 项已修补，待第 3 轮复审）

---

## 0. 新窗口先读

1. `PROJECT_STATE.md`（本快照会同步更新）
2. `DECISIONS.md` → **D-009**
3. `work/2026-08-27-decision-semantics-workflow.md`（派工入口）
4. `work/2026-08-27-audit-decision-semantics-plan.md`（权威审计）
5. 治理章程上传件：`TradingAgents-AShare_Cursor_Multica…2026-08-27`（Cursor 总控 / Multica 实施；后续功能施工默认走团队，本卡 WIP 是 Cursor 已落地的本地改动）

开工前用 `git status` / `git rev-parse HEAD` 重核，勿把本文件当实时 Git 真相。

---

## 1. 卡目标（P0-1）

堵住「7/7 上游失败 → Neutral/HOLD/completed 进校准」的语义坍缩。

验收钉子（R3 形态）：七分析师失败 → `INVALID_RUN` + `NO_TRADE` + `direction=N/A`；无置信度/概率/目标价；不进校准；辩论/交易/风控 LLM 不再跑。

---

## 2. 已落地（未 commit）

### 核心模块（新文件）
- `tradingagents/agents/utils/decision_status.py` — 状态机、`valid/partial/abstain/invalid`、双周期聚合、校准资格、风控终态映射
- `tradingagents/agents/utils/run_integrity.py` — 严格失败判定、`Run Integrity Gate`、INVALID 终端 payload

### 图路由
- `graph/setup.py`：七分析师 barrier → **`Run Integrity Gate`** → Bull **或 END**
- `graph/conditional_logic.py`：`should_continue_after_integrity`
- Manager / Trader / Risk：非可执行状态短路；Risk 终态重写 canonical

### API / DB / 校准 / 前端
- ReportDB 列 + 方言迁移（关键列优先；PG `BOOLEAN DEFAULT FALSE`；迁移后 re-inspect）
- 双周期 top-level 聚合落库
- 校准：`analysis_status == VALID` 才入选（**NULL 历史排除**）
- 前端：`DecisionAction` + DecisionCard 独立展示 invalid/no_trade/watch；`tsc && vite build` 已绿

### 测试（新/改）
- `tests/test_run_integrity.py`
- `tests/test_decision_status.py`
- `tests/test_research_manager_run_integrity.py`
- `tests/test_p0_1_review_fixes.py`
- `tests/test_report_schema_migration.py`
- `frontend/src/components/DecisionCard.test.tsx`

最近定向验证（本机）：
- Python 相关套件：**94 passed**
- Frontend DecisionCard/reportText：**17 passed**
- Frontend：**`npm run build`（tsc && vite build）通过**

---

## 3. 两轮审查对照（勿回退）

| 轮次 | 结论 | 关键修补 |
|---|---|---|
| 第 1 轮 | 不批提交（6 High） | 闸前移；严格失败文本；双周期聚合；VALID/PARTIAL；DB/API/UI；方言迁移 |
| 第 2 轮 | 不批提交（4 High） | 前端编译+DecisionCard；consistency→ABSTAIN；Risk 重写状态；校准剔 NULL；PG DDL/顺序 |

**尚未批准提交。** 新窗口默认：先独立复审或按 David 指示，再考虑 commit。

---

## 4. 禁止动的脏文件（非本卡）

除非任务明确授权，**勿改 / 勿暂存 / 勿提交**：

- `AGENTS.md`（已有无关本地 diff）
- `frontend/src/services/api.ts`（本地 `v2_debate_enabled` override）
- `work/h1b_gates_report.json`

P0-1 commit 时必须把上述文件排除在外。

---

## 5. 建议 commit 范围（批准后）

**纳入（示意）**
- `tradingagents/agents/utils/decision_status.py`（新）
- `tradingagents/agents/utils/run_integrity.py`（新）
- `tradingagents/agents/utils/agent_states.py`
- `tradingagents/agents/utils/debate_utils.py`
- `tradingagents/agents/managers/research_manager.py`
- `tradingagents/agents/managers/risk_manager.py`
- `tradingagents/agents/trader/trader.py`
- `tradingagents/graph/{setup,conditional_logic,propagation,trading_graph,signal_processing}.py`
- `api/{database,main}.py`
- `api/services/{report_service,calibration_service}.py`
- `frontend/src/{components/DecisionCard.tsx,components/DecisionCard.test.tsx,pages/Analysis.tsx,pages/Reports.tsx,types/index.ts,utils/reportText.ts}`
- `tests/test_{run_integrity,decision_status,research_manager_run_integrity,p0_1_review_fixes,report_schema_migration}.py`
- `tests/test_custom_prompt_injection.py`（仅 integrity mock 一行）
- 可选文档：`PROJECT_STATE.md`、`DECISIONS.md`、`work/2026-08-27-decision-semantics-workflow.md`

**排除**：§4 三脏文件；社交 Task 5；任何无关 untracked 海量 `work/comment-*`。

一次关注点：仅 P0-1。不混 P0-2 / 社交。

---

## 6. 新窗口下一步（按优先级）

1. **独立复审 P0-1**（或 David 明确「可提交」）→ 按 §5 单卡 commit  
2. **P0-2**：EvidenceRecord + PIT 防火墙（`effective_as_of > cutoff` 拒入方向 claim）  
3. 社交 Task 5 可并行基建，**不抢 P0、不混 commit、不得提前 active**  
4. 遵守新治理：功能施工优先 Multica；Cursor 总控/验收；未证明不宣称完成

---

## 7. 已知边界 / 不确定点

- Integrity Gate 挡的是**分析师之后**的辩论/交易/风控；七分析师 LLM 本身仍会跑完（502 已发生）。
- 全图 e2e「真实 LLM 计数」未做；现有的是 Gate 迷你图 + Trader/Risk astream=0 单测。
- R1/R2/R3 冻结 fixture 仍未齐 → **不得宣称蓝思案例已修**。
- `data_collector` 数据层完整性摘要、backtest 隔离（P1-3）不在本卡完成定义内。

---

## 8. 一键自检命令

```bash
cd /Users/davidliu/Documents/TradingAgents-AShare
git rev-parse HEAD
git status -sb

python3 -m pytest \
  tests/test_run_integrity.py \
  tests/test_decision_status.py \
  tests/test_research_manager_run_integrity.py \
  tests/test_p0_1_review_fixes.py \
  tests/test_report_schema_migration.py \
  -q

cd frontend && npm test -- --run src/components/DecisionCard.test.tsx src/utils/reportText.test.ts && npm run build
```
