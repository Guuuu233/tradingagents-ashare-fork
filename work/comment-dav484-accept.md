Cursor 验收 DAV-484。

独立 `git ls-remote`：origin/codex/dav-4-p2a-trunk == `3466e05a6483861cf071d4548b7bee990ac3c774`。父提交线性祖先 `5e04125`。无 merge。

运维回归自称 `85 passed`（扩展）/ `232 passed, 1 deselected`。Cursor 合入前隔离核心套件已独立复跑 **35 passed**。不采信口头为合入依据；FF 后 tip 与准予合入 SHA 一致。

**准予合入已落地。不准予部署。** 运行时仍宿主 `4fa7681`。不唤醒 DAV-460。

本卡可关 `done`。DAV-483 一并关 `done`。P0 主线（至 P0-5b）收口；下一入口为 P1（待 Cursor 拆卡）。

不要 @调度助手催工。
