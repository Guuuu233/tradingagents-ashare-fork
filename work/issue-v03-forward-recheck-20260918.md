# DAV-1044：V-03 FORWARD_OOS 触发条件复核（只读）

## 目标

刷新 V-03 前向样本状态，判断当前是否已经达到可评测规模；若达到，则在生产库只读副本上跑 FORWARD_OOS；若未达到，明确写出缺口和下一次触发条件。不得把“有 1 条样本”自动写成实验结论，也不得把历史 OOS 与前向 OOS 混在一起。

## 当前基线

- 代码/运行基线：`6ee148699339efefc2f7f7548eb286be485524e1`
- 目标账号：`429163f7-50b6-4982-8bdf-96ae99506843`
- 分界：`FORWARD_OOS >= 2026-09-09`
- 当前只读核验已知：该账号 09-09 之后有 2 条 completed 报告，其中 09-10 一条已有 T+1 实际值，09-17 一条仍为 `【数据缺失】`、评估日为 09-18。

## 硬边界

- 只能对生产库 `.backup()` 到临时副本后读取；备份前后 SHA/计数/`quick_check` 必须记录。
- 不写 `data/tradingagents.db`，不调用 API 真实分析，不产生新报告，不启动真实社交采集，不使用 Cookie，不改 `credit_weighting_enabled`，不部署、不合入。
- 使用 `/Users/davidliu/Documents/TradingAgents-AShare/.venv310/bin/python`（Python 3.10.20）并清理 `PYTHONPATH` 与代理变量。
- 如果项目现行协议没有明确“可评测规模”的数字，必须如实报告“阈值未被明确钉死”，不得自行补一个阈值并据此宣称通过。

## 交付

报告写清：

1. 实际代码 SHA、解释器和副本路径；
2. 09-09 之后 completed、已评估、pending 的精确数量及每条排除原因；
3. 是否达到现行协议的可评测规模；若达到，给出 FORWARD_OOS 样本、收益字段、交易成本、PIT/前视检查和原始日志；若未达到，给出可复核的下一触发条件；
4. 生产库零写入证据；
5. 产物统一使用 `work/v03-forward-recheck-20260918-` 前缀。

这是只读评估任务，不需要代码审查卡；完成后把结论留在 DAV-1044，是否进入下一轮实验由总工依据证据决定。
