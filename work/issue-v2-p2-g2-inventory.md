# P2-G2 只读：数据供弹缺口盘点（不开 writer）

## 基线

主干/服务 `f8f624241d125125a61c69454638982f2423740f`。规格 10.3。P2-G1 已上线。本卡只读，禁止改代码、provider、3/1、模型绑定，禁止新开分析刷样本。

## 要做

对照规格 10.3，在当前代码与最近 v2 报告 provenance 里列出：

1. `fund_flow_individual`、`share_pledge`、财报 as-of：是否有真实来源、as-of、失败类型。
2. 北向明细是否仍被当成可用；若是，应记 known-gap，不得伪称北向。机构动向是否只用真实 ETF 份额/两融。
3. 历史板块即时快照是否仍在尝试。
4. 五战场数据清单 vs 实际 provenance 不一致处。
5. `data_gaps` 常驻数量粗计（若可得）。

产出一张表：字段 / 现状 / 证据（文件:行 或 report id）/ 建议下一张 writer 卡是否该开、建议允许文件。不要自己改代码。n<10 的 DAV-421 战场验收与本卡无关。

不要 mention 项目调度助手。
