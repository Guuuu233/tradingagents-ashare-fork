# DAV-595 开工 brief（卡B：数值证据伪 contradiction）

**父 tip（必须从此 SHA 建隔离 worktree，禁止用 c72dd7b）：**  
`e10b106df9d3173258b0a3fefc90ba7f3559f109`

远端主干 `origin/codex/dav-4-p2a-trunk` 已线性 FF 到上述 SHA。

## 背景

美的 `INV-6` 证据聚合出现 `contradicted:1`。可疑：复合句里「应收账款增长 17.10%」「营收增长 3.55%」被宽泛关键词匹配到约 25% 毛利率等**不同指标**，形成伪 contradiction。

与 confirmation-gate 生命周期正交。593 已合入，本卡只修匹配。

## 范围（一个 commit）

原路径：`tradingagents/agents/utils/evidence_verifier.py`（及现有测试/必要 fixture）。

- 数字必须与具体指标名绑定，不能只靠同行共有词
- 复合句拆成原子 evidence
- 同一行不同指标不得串值
- 百分比须期间、单位、指标名一致后才能判冲突

RED 测试必须在旧匹配逻辑上失败、修复后通过。

## 禁止

- 再改 `decision_status.py`（593 已合入）
- 部署、重启生产、改 DB、改 `credit_weighting_enabled`
- merge commit、自动 FF
- 宿主脏 checkout 施工
- H1b 补样本

## 验收

1. 复现或证伪 INV-6 伪 contradiction（贴匹配路径）。
2. 若属实：原路径修复 + 数字串值负例 / 真冲突正例。
3. 报告：完整 pytest 命令、退出码、测试数量、40 字符 SHA、changed files、`git status`、`git diff --check`。
4. D-010：独立审核 exact SHA → Cursor 准予合入 → 另卡 FF。
