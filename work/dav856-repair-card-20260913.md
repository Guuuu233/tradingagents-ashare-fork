# DAV-856 修复卡：恢复既有快照路径测试强度

## 目标

在候选 `b92acd15bc20d6a1bcc699f3ea7723fcb8868b37` 之上做最小修复，消除代码审查门禁发现的测试契约回退：`tests/test_custom_prompt_injection.py::test_T14_both_save_branches_call_attach_snapshot` 不得从严格的 `source.count(call) == 3` 放宽为 `>= 3`。

## 基线与边界

- 修复父提交必须是 `b92acd15bc20d6a1bcc699f3ea7723fcb8868b37`。
- 当前施工主干仍为 `54077b6ad4a287bbd8c43d386e91427662a5e786`；本卡不直接改主干。
- 不得改变 D-015 六项产品契约、三层守卫、整单 `NO_TRADE`、五角色不注入或任何生产提示词基线。
- 不得合入、部署、重启、写生产库、真实采集或修改配置。

## 必修内容

1. 恢复既有 T14 测试的严格断言，不能用 `>=`、删除断言、跳过测试或其他方式降低原有覆盖。
2. 如果新增的 fail-closed 分支需要快照覆盖，为该新分支增加独立测试；不能以放松既有三条原路径断言来容纳实现。
3. 生产代码只允许在 DAV-856 原白名单内做必要的最小整理；测试可以在原白名单测试文件中新增或恢复，但不得删除既有行为契约。
4. 交付必须说明为何修复后原有三条保存路径仍各自存在，以及阻断分支如何单独验证。

## 验收

- `test_T14_both_save_branches_call_attach_snapshot` 恢复 `== 3` 并通过。
- DAV-856 的 RT-1～RT-16 逐条实跑仍全部通过，特别是 RT-11、RT-15 的报告回读和 0 LLM 证据不变。
- 运行 custom prompt 三层专项、相关生命周期专项，全部通过。
- 从 `b92acd15bc20d6a1bcc699f3ea7723fcb8868b37` 到修复候选重新跑全量 `pytest -q -p no:randomly`；失败集合不得比 DAV-856 候选新增，且 DAV-850 的两项回归测试保持通过。
- 新候选提交必须提供完整 40 位 SHA、第一父、远端 ref、白名单 diff、`git diff --check` 和干净工作树。
- 修复后必须再次创建代码审查任务并派给 `代码审核员`，针对修复后的同一完整 SHA 只读审查；原 DAV-858 的 PASS 不得直接复用。
