**只读复核卡（总工派工，2026-09-12）。依据 D-012 §5b：红队清单的完备性须经第二双眼睛确认。**

## 为什么有这张卡

D-012 §5b 的立法起因是 **DAV-789**：开卡方单方列 `red_team_scenarios`，只列了「不传取价源」一个入口，漏了「取价源给了但没返回有效值」（部分 `price_series` / `get_price_fn` 返回 None）两个入口，导致候选与审核双双漏过真缺陷，最后由另一执行位补出。

**本次总工一次性开了三张实施卡，各自附了红队清单，三张清单都是同一个人（总工）列的。这正是 D-012 §5b 要防的情形。**

## 任务（只读，不实现）

复核以下三张卡的 `red_team_scenarios` **覆盖面是否完整**，重点是「**同一缺陷的多个入口是否都列全**」：

| 卡 | 主题 | 基线 | 清单位置 |
|---|---|---|---|
| DAV-828 | E-01 producer（Claim-to-Claim 路径） | `70b5b47` | 卡内 8 条 RT + RT-FULL |
| DAV-829 | L2 博弈论生产接线 | `70b5b47` | 卡内 7 条 RT + RT-FULL |
| DAV-830 | L3 V-01-2 真实结算管道 | `70b5b47` | 卡内 8 条 RT + RT-FULL |

## 复核方法（必须实做，不得静态通读）

1. 在**精确基线 `70b5b47bcff0618e0db1258a438b0875440d4ac5`** 的隔离 worktree 中打开相关源码，逐条核对清单条目对应的**契约依据行号是否真实存在**。
2. 对每张卡，回答三个问题：
   - **入口枚举**：该关注点在代码中一共有几个入口 / 几条调用路径？清单覆盖了几个？（DAV-789 的教训：入口数 > 1 时最易漏）
   - **回退路径**：是否存在「主路径正常、回退路径绕过校验」的形态？清单是否覆盖回退路径？
   - **fail-closed 边界**：清单里的「必须拒绝」类断言，是否覆盖了所有会导致静默降级的输入形态？
3. 逐卡给出：**覆盖完整 / 建议补充 N 条（逐条写出场景、契约依据、预期）**。

## 已知需要重点核的三个点（总工自陈的不确定处）

- **DAV-828 RT-6**：清单写「不得生成边」，但**是否覆盖了「关系可证明但被下游 reducer 误折叠」这一相反方向的缺陷**？请核 `reduce_evidence_claims` 的折叠分支。
- **DAV-829 RT-3**：清单只写「节点失败不得中断流程」，但**是否覆盖「节点部分成功、写入了不完整 state」** 的形态？
- **DAV-830 RT-5**：总工已裁定分红/送转走「结果字段」而非新增状态。请核**该裁定是否与 `HorizonReturnResult` 现有字段语义自洽**，以及清单是否遗漏「分红数据存在但送转数据缺失」的半缺失形态。

## 允许改

无。**本卡为只读，禁止修改任何文件、提交、推送、FF、部署、重启、写库、真实 provider/LLM 调用。**

## 交付

在卡内评论给出：三张卡的逐条覆盖结论 + 建议补充清单（若有）+ 核对所用的精确 SHA 与 worktree 路径 + 工作树状态。若认为某张卡清单已完备，明确写「无补充」并说明已枚举的入口数量与依据。

## 硬约束

不得改写被复核卡的红队清单（那是开卡方的职责，你只出结论）；不得直接进入实现审核——本卡只确认**清单完备性**，实现审核另卡。
