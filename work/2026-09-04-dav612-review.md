# 独立审核：DAV-612 source_hash 跨源对齐

**只读。** 禁止改功能代码、禁止 commit、禁止 push、禁止部署、禁止 FF。

## 候选

- **exact SHA：** `9b3de9b0153e4727896692e42d22223a9b0f4efe`
- **分支：** `origin/agent/1/8def9b4761b9`
- **父 tip：** `158ccb7e9df49ba0d0510d1a50b4cc82c6c3ed55`
- **brief：** `work/2026-09-04-dav612-url-source-hash.md`

## 验收

1. `git diff --name-only` 仅允许：
   - `tradingagents/dataflows/news_event_evidence.py`
   - `tests/test_news_event_coverage.py`
2. 有规范化 URL、不同 source → 相同 `source_hash`。无 URL、不同 source → 哈希仍不同。
3. 无 `canonical_event_id`，无新 provider。
4. 隔离 pytest，写命令/退出码/数量。对上述 40 位 SHA 给出 ✅通过 / ❌打回。

独立审核 PASS ≠ 准予合入。
