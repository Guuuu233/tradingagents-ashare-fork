## 目标
优化与清理执行：按 H1/H2 审计报告落地——删死代码、合并重复逻辑、清理未用导入与注释代码、清理遗留并行实现、依赖体检、文档同步。

## 背景
- 主干：`codex/dav-4-p2a-trunk` HEAD `3c339c5`（M1~M5 已竣工）
- 规划文档：`work/2026-08-05-code-audit-plan.md`（必读）
- **前置依赖：H1 审计 + H2 测试体检均完成并验收**（审计报告 `work/code-audit-report.md` 为施工依据）
- 施工纪律：见规划第 4 节

## 任务清单（以审计报告条目为施工依据，逐条销号）
1. 删除死代码：每条删除必须可回溯到审计报告条目（评论中引用条目编号）
2. 合并重复逻辑、清理未用导入、移除注释掉的代码块
3. 清理遗留并行实现（_v2/_old/_new/_fixed 等）：合并到原路径后删除
4. 依赖体检：移除未使用依赖（有据可查）
5. 文档同步：README/CHANGELOG/KNOWN_ISSUES 与实际代码一致；过期文档更新或标注
6. 处理 H2 登记的覆盖缺口（补关键测试）

## 纪律与验收标准
- **一个 commit 一个关注点**，commit message 引用审计报告条目编号（如 `clean(api): remove dead code, audit #P0-03`）
- 每个清理后跑相关测试套件；全部完成后跑全量回归：**0 新增失败**
- 禁止删除仍被引用的代码（读完再改、grep 调用点）
- 不自行提交主干；完成后评论 @项目调度助手，附清理统计（删了多少行/文件、合并了什么），等待 Hermes 验收后放行 H4

## 环境铁律
- 所有 Python 命令必须 `env -u PYTHONPATH`；测试用 `.venv310/bin/python -m pytest`
- 发现审计报告遗漏的问题：先补充登记到报告，再处理
