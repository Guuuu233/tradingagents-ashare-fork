# DAV-254 主干 fast-forward 合入 0b10041

Hermes 代表用户授权合入。独立终审 DAV-253 PASS。

## 精确门闩

- 当前主干必须仍是：`target/codex/dav-4-p2a-trunk@c956e43db0fea1fea88b85103d10218734b2b1c8`
- 候选：`target/integration/dav248-dav249@0b10041f9e68b5d0116b76c36cd629acb365f10d`
- 父链：`c956e43 → 77cd6ac`（DAV-249 astream 吞异常）`→ 0b10041`（DAV-248 资金流误阻断）
- `ahead 2 / behind 0`，可 clean fast-forward
- 仅 5 文件：`api/main.py`、`tests/test_job_lifecycle.py`、`tradingagents/dataflows/fund_flow_evidence.py`、`tests/test_fund_flow_evidence.py`、`tests/test_smart_money_fund_flow_semantics.py`

## 操作

1. 独立 checkout，禁止改宿主工作树、禁止 `reset --hard` / `clean -fd`、禁止碰 `.env` / `uv.lock`。
2. 再次 `git ls-remote` / GitHub API 确认主干仍是 `c956e43`、候选仍是 `0b10041`。
3. `git merge-base --is-ancestor c956e43 0b10041` 必须成立。
4. 对 `codex/dav-4-p2a-trunk` 做 **fast-forward only** 到 `0b10041`。禁止 merge commit、禁止 cherry-pick 到主干、禁止强推。
5. 用 GitHub API + `git ls-remote` 回读主干 HEAD = `0b10041f9e68b5d0116b76c36cd629acb365f10d`。

## 禁止

- 不部署、不重启 8000、不跑真实 3/3
- 不改用户模型/providers/Key/持久轮次
- 不把基线既有失败 `test_fundamentals_analyst_node_with_collector_pool` 当成本次合入阻塞（终审已核验主干同样失败、不在 5 文件内）

## 交付

主干精确 SHA、父链、ahead/behind、5 文件确认。明确未部署、未复验。不得 @项目调度助手。立即执行。
