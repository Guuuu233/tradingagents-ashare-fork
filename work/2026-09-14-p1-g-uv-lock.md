# P1-G `uv.lock` 对齐与合入证据

## 结论

P1-G（DAV-913）已完成并以 fast-forward 方式合入
`origin/codex/dav-4-p2a-trunk`。这是依赖锁文件的元数据修复，不改变 Python、前端、Docker、数据库或运行进程，因此不单独部署、不重启服务。

## 对象与范围

- 基线/直接父：`78d7b09a2e1f664d9d5e44ee581660a16ef734f3`
- 候选：`054a76210f799029fe8d390512c763c36f0fb585`
- 远端候选分支：`origin/agent/1/69b0dce5be07`
- 变更清单：仅 `M uv.lock`（32 insertions, 673 deletions）
- `git diff --check`：通过
- 合入方式：`git merge --ff-only`；合入后目标主线为候选 SHA

本次锁文件变化与当前 `pyproject.toml` 对齐：项目版本为 `0.6.0`；移除
`langchain-experimental`；包含 `python-dotenv`；dev 依赖包含 `fakeredis`。
新增锁定包为 `fakeredis==2.38.0` 与 `sortedcontainers==2.4.0`；其余移除项是
不再由当前声明引用的传递依赖，未发现无关的既有依赖版本升级或降级。

## 门禁证据

在候选隔离 worktree 中，依赖解析器实际使用 CPython `3.13.15`，以下门禁均退出码 `0`：

- `uv lock --check`：`Resolved 114 packages`
- `uv sync --frozen --dry-run`：`Would download 1 package`、`Would install 106 packages`
- 候选 worktree clean

## 独立审查

**代码审核员**对同一完整候选 SHA 做只读复核并给出 PASS，核对了唯一文件范围、
项目元数据、锁包差异、两个依赖门禁和 `git diff --check`。审查没有修改、提交、
合入、部署、重启服务、写生产库或进行真实采集。

## 边界

- 已合入主线，但不产生新的运行时代码；线上服务仍为发布 SHA
  `6cc4e15227efcb602d63f1ec9a49a4d7ca7cc8e1`。
- 未运行 Python 全量测试：本次只有依赖锁文件变更，验收采用锁一致性和冻结安装预演门禁。
- 未部署、未重启、未写生产数据库、未进行真实分析或真实社交采集。
