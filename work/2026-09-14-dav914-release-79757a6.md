# DAV-914 主线发布收口

日期：2026-09-14（Australia/Perth）

## 发布对象

- 目标主线完整 SHA：`79757a6a2dd98f9487bb1fed6466ba71e7e6a31a`
- 该提交的代码父为 DAV-914 候选 `8ccecb8d59de31835f9d0f67578123db9a358e7b`；其上一线上版本为 `6cc4e15227efcb602d63f1ec9a49a4d7ca7cc8e1`。
- 发布副本：`/private/tmp/ta-release-main-79757a6-20260914`
- 前端在该发布副本重新执行 `npm test -- --run`（17 个文件/171 个测试全过）和 `npm run build`；生成的 live bundle 为 `/assets/index-kJZ_I3MN.js`。

## 发布前门禁

- 生产 SQLite：`/Users/davidliu/Documents/TradingAgents-AShare/data/tradingagents.db`
- 发布前大小：`285339648` bytes；SHA-256：`94d2f6740db4f2065100479dd5cb3ccf5d8a504447a55fa8f19635927ce83010`
- 发布前 `quick_check`、`integrity_check` 均为 `ok`；reports 为 `completed=793`、`failed=616`。
- 备份：`work/tradingagents.db.bak-20260914-predeploy-79757a6`；备份检查均为 `ok`，reports 仍为 `793/616`；备份 SHA-256：`df628f4293069a820ef69a136a1acbb0e7a517f6638dd24a9faeea30f5b51b9e`。
- 先在 8001 预启动，`/healthz` 精确回读 `79757a6a2dd98f9487bb1fed6466ba71e7e6a31a` 后才切换 8000。

## 上线后只读核验

- 8000 当前 PID：`42225`；父进程：`71186`；工作目录：`/private/tmp/ta-release-main-79757a6-20260914`。
- `/healthz` HTTP 200，`commit_sha` 和 `build_identity` 均精确为 `79757a6a2dd98f9487bb1fed6466ba71e7e6a31a`，队列为 0。
- `/v1/dataproviders/health`：`status=ok`；非占位 provider 均 healthy。
- 只读 K 线 `600519.SH`（2026-09-08 至 2026-09-09）返回两行；`/v1/social-data/status` 仍为 `disabled`；未知 API 路径返回 404。
- `/` 返回 Dashboard 页面 200，`/assets/index-kJZ_I3MN.js` 返回 200，资源 SHA-256 为 `1b4ecee96ad515f8ecbefe1442fa6dc1ed9be4eb4e21b8bedf0e2951d9cdf937`，bundle 中存在本地化文案标记。
- 上线后数据库仍为 `285339648` bytes、SHA-256 `94d2f6740db4f2065100479dd5cb3ccf5d8a504447a55fa8f19635927ce83010`；`quick_check/integrity_check=ok`；reports 仍为 `793/616`。
- 8001 预启动进程已正常关闭；旧发布副本保留用于回退核验。

## 边界

本次只发布已审查合入的代码和前端 bundle，未启动真实分析、未写生产报告、未采集真实社交数据、未启用信用加权，也未改写历史数据。生产 Compose 没有在本次 uvicorn 发布中启动；P2-65 的容器挂载效果仍需在实际使用 Compose 时单独做容器级核验。
