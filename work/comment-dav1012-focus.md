## 复审重点补充（六项）+ 一处主控口径更正

被审对象：完整 SHA **`4cc436ac058b3d4a57c2e52ab3c6da3ab76ff96b`**

---

### 〇、主控口径更正

我此前说「复审通过后**即可锁定** A+B 组合 SHA」——**说早了**。

准确流程：**复审 PASS → 重新确认 SHA / HEAD / 工作树均未变化 → 才正式锁定 A+B 组合 SHA → 再启动唯一一次完整离线全量。**

**当前不得提前锁定组合候选。**

### 一、以下六项请重点复核

| # | 复核点 |
|---|---|
| ① | **五文件累计 diff 与两个关注点是否严格对应**——每一处改动都能归到 B1（baostock 集成加固）或 B2（拒绝传播语义）之一；有无夹带 |
| ② | **加固补丁是否幂等**，重复调用是否安全；失败时**是否留下错误的模块级状态**（`_HARDENING_INSTALLED` / `_HARDENING_ERROR` 半安装态） |
| ③ | **AST 守卫是否覆盖动态字面量导入**（如 `__import__("baostock")`、`importlib.import_module("baostock")`）**及全部生产目录**（`tradingagents/`、`api/`、`scheduler/`、`scripts/`） |
| ④ | **v03 的拒绝穿透与普通 `None` 语义是否同时保持**——`NetworkAccessDeniedError` 必须抛出，而「普通数据不可用」仍返回 `None` |
| ⑤ | **`elapsed < 0.2` 断言是否存在机器负载导致的脆弱性**——CI/并发下是否会偶发失败；若脆弱，给出更稳的判据建议 |
| ⑥ | **三类路径是否真正分流**：普通 `TimeoutError`（保持 45 秒重试）、非零 login 结果（`NotImplementedError`，不重试转下一 vendor）、明确拒绝（`NetworkAccessDeniedError`，不重试不 fallback 立即上传播） |

### 二、口径提醒

- ②③ 属于**结构性**问题：只看测试绿不绿看不出来，必须**读实现**
- ⑤ 是**测试质量**问题：现在绿不代表稳定绿，**时间阈值断言容易在负载下假红**
- ⑥ 要验的是**分流本身**，不是「三个用例都绿」——请确认它们**各自走的是不同分支**，而非殊途同归

### 三、纪律

只读复审：不得修改代码、不得提交、不得合入、不得部署、不得重启服务、不得写生产库。
输出 🔴阻断 / 🟡建议 / 🟢认可，每条给**文件:行号**与依据。

**本卡不构成合入授权。** 主线 `5a0320f` 保持 HOLD。
