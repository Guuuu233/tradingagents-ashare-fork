# 卡B：证据数字匹配专项审计（伪 contradiction）

**日期**：2026-09-04  
**依赖**：卡A（confirmation lifecycle）合入前可并行**只读审计**；**改代码须等卡A 合入或明确文件不重叠**。  
**父 tip（开工写代码时重钉最新主干 tip）**：当前参考 `c72dd7b6098297efbec80931dda8bf509c8d8709`

## 背景

美的 `INV-6` 证据聚合出现 `contradicted:1`。可疑点：复合句中的「应收账款增长 17.10%」「营收增长 3.55%」可能被宽泛关键词匹配到报告里约 25% 毛利率等**不同指标**，形成**伪 contradiction**。

此问题与确认闸「是否应把 rejected+reject 当 global fatal」正交：即使卡A修好闸门，伪 contradiction 仍污染审计质量与未来 adopted 路径。

## 范围

只读优先，再窄改证据匹配：

- 数字必须与**具体指标名**绑定，不能只靠同行共有词；
- 复合句拆成原子 evidence；
- 同一行不同指标不得串值；
- 百分比须期间、单位、指标名一致后才能判冲突。

**禁止**与卡A 混在同一 commit / 同一大改里。

## 验收

1. 复现或证伪美的 INV-6 伪 contradiction（贴匹配路径与证据）。  
2. 若属实：原路径修复 + 定向测试（数字串值负例 / 真冲突正例）。  
3. 文末 40 字符 SHA；D-010 流水线。

## 纪律

不部署、不开加权、不改 3/1。从当时最新 tip 建隔离分支。
