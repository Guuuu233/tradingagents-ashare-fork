# 独立审核：DAV-608 event_coverage 召回诚实化

**只读。** 禁止改功能代码、禁止 commit、禁止 push、禁止部署、禁止 FF。

## 候选

- **exact SHA：** `4f6e45ec4a9a87da32c24ebfaea92f9ecf66dd9d`
- **分支：** `origin/agent/1/bf9d962ca457`
- **父 tip：** `90fcfc435ccc4f4efd32d5ac5c4fbfbfe45975e9`（必须是该 SHA 的第一父）
- **brief：** `work/2026-09-04-dav608-event-coverage-recall-honest.md`

## 验收

1. `git merge-base` 证明祖先含主干 tip；`git diff --name-only` 仅允许：
   - `tradingagents/dataflows/news_event_evidence.py`
   - `tradingagents/graph/data_collector.py`
   - `tradingagents/agents/analysts/news_analyst.py`
   - `tests/test_news_event_coverage.py`
2. 无应查清单 → `recall_status=unknown`，不得编造五主题；summary 不得含「无明显主题缺失」。
3. 无 CNINFO/IR/`canonical_event_id`/新 provider。
4. 隔离复跑 pytest，写命令、退出码、数量。对 **上述 40 位 SHA** 给出 ✅通过 / ❌打回。

独立审核 PASS ≠ 准予合入。Cursor 另做同 SHA 复测。
