# P2-G1 部署

等 `target/codex/dav-4-p2a-trunk` 精确等于 `f8f624241d125125a61c69454638982f2423740f`。若仍是 `4fa2e24` → BLOCK。

项目根 `/Users/davidliu/Documents/TradingAgents-AShare`。在途报告 0。禁止 hard reset。FF 宿主到该 SHA。DATABASE_URL=`sqlite:///./data/tradingagents.db`，`.venv310`，代理 + 完整 no_proxy，重启 uvicorn。`/healthz.commit_sha` 必须等于 `f8f624241d12…`。3/1 不变。

不要 mention 项目调度助手。
