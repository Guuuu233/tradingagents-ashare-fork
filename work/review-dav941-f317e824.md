# DAV-941 候选 `f317e824` 只读代码复审

请由 **代码审核员** 对下面这个完整候选 SHA 做同一版本的只读复审。审查不修改、不合入、不部署；不要使用“独立代码审核员”。

## 版本边界

- 实施卡：DAV-941（历史案例 T+1 行情拒绝冲突重复日）
- 候选完整 SHA：`f317e82431ab4d950a599e6ebd304cc3e53f1e51`
- 直接父 SHA：`b95a9b88c81e87a4da121f9945aedbe844fa04e0`
- 远端分支：`origin/agent/1/01a0a2b8-dav-941`
- 目标主线：`origin/codex/dav-4-p2a-trunk`，交付时 HEAD 为上述直接父

## 严格白名单

1. `tradingagents/knowledge/historical_cases.py`
2. `tests/test_historical_cases.py`

越出白名单、父提交不一致、候选 SHA 不一致或检出工作树非 clean，直接打回。

## 复审重点

- 历史案例 T+1 行情解析不得再按返回顺序让后来的行覆盖同一交易日的前一行；同日冲突值必须拒绝（fail-closed），不得静默取第一条或最后一条。
- 同日完全相同的重复行可确定性折叠，但折叠与“冲突拒绝”必须分开实现且各有测试。
- 拒绝路径必须产生可观测的显式缺口，不得把拒绝降级为静默空值，从而让案例复盘的涨跌幅凭空变化。
- 与既有 DAV-936（收益链消费者）/DAV-935（K 线接口）的去重语义保持一致口径，不得引入第二套矛盾规则。

## 红队场景（须逐条实跑并贴输出）

- RT-1 同日两行冲突收盘价 → 拒绝并显式缺口。
- RT-2 同日两行完全相同 → 确定性折叠为一行，数值不变。
- RT-3 正常无重复序列 → 行为与修复前一致（无回归）。
- RT-4 空/缺列/非法日期输入 → 沿用既有拒绝语义，不抛未捕获异常。

## 须独立复核的证据

- 固定 Python 3.10 环境，`env -u PYTHONPATH`。
- 精确 collected / passed / failed / skipped / deselected 数字。
- 本刀改动产品代码，须按 D-012 §4b 提供 RT-FULL 全量回归并与线上基线失败集合逐项对照；有任何新增失败即不得 PASS。
- 全程离线 fixture，不请求真实供应商、不调用模型、不写数据库、不启动或重启服务。

请只读检出并复测完整 SHA，给出路径/行号、精确测试数字和明确评级（PASS / FAIL）。
