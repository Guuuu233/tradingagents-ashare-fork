# 施工表与当前状态对照（2026-09-12）

依据：`work/2026-09-12-dispatch-sequence.md`。该表自身声明是只读派工建议，不是派工、合入、部署、重启或真实采集授权；其开头的 `220e253` / `4a5206f` 快照已过时，以下以实时核验为准。

## 当前排序

| 施工表位置 | 当前事实 | 处理 |
|---|---|---|
| 0-A 部署 | 服务仍为 `70b5b47...`，远端施工主干已推进到 `7810f198...`；表中 0-A 标注需 David，且部署后还要按实际 SHA 验收 | 未执行；不因合入授权自动部署 |
| 0-B D-04 | 已由 `70b5b47...` 合入并回读 | 已收口 |
| 0-C daemon | `multica daemon status` 为 running，PID `47460` | 已满足运行态前置 |
| L1 E-01 | `DAV-828` 已合入 `21140a...` | 已收口 |
| L2 博弈论接线 | `DAV-829` 已合入 `a72daa...` | 已收口 |
| L3 真实结算 | `DAV-830` 已合入 `41b77dc...` | 已收口 |
| L4 B-01 | 只读清单已核验；未找到真实 MediaCrawler / source DB / archive，且未获真实采集授权 | 报告为 blocked；不宣称 Gate 0 完成 |
| L5 量价填充率 | 不依赖 0-A 或 DAV-808；允许范围是 providers 与 `graph/data_collector.py` 及测试 | DAV-844 已按精确 SHA `59713d3744bc4563e34a59173c4f12a16901bc9d` 审查并线性 FF；父子 RT-FULL 均 19 项既有失败，零新增 |
| 2-1 E-02 生效验证 | L1 依赖已满足；原则上只读验证，只有发现 seam 缺陷才改码。施工表旧有“独立观察 retain increment”措辞已被 DAV-807 / DAV-806 的后续裁定收紧：未连接观察保留 `pending/unknown`，未明确独立性前不计新增贡献，沿用 0/1 上限 | 已按后续裁定只读验证：155 passed；没有发现 seam 缺陷，不开返修 |
| 2-2 E-03b-1 / E-03b-2 / E-03c / E-03d | E-03a、H-05a、H-04d、E-02 前置均已在当前 trunk 祖先链；施工表顺序是 Bull → Bear → E-03c → E-03d | `DAV-846` / `DAV-847`、`DAV-848` / `DAV-849` 已收口并线性合入；E-03c `DAV-850` 候选 `1ef79a736334a0bad5022e510387dead69c69666`（父 `33d81aee1a17d9b7dd995d18ef32aa3e706e51b4`，remote ref `origin/agent/1/01a0955d`）已由代码审核员 `DAV-851` 同 SHA PASS 后，从父提交非强制线性合入远端 trunk。DAV-850 五项最低回归在指定 Python 3.10 下 `110 passed`，白名单 5 文件、`git diff --check` 通过；E-03d 候选 `DAV-852` `7810f19875890725cd26e414cde272063fb3606a`（父 `1ef79a736334a0bad5022e510387dead69c69666`，remote ref `origin/agent/senior-dev-1/dav852-e03d-research-manager`）经代码审核员 `DAV-853` 同 SHA PASS，白名单 2 文件、`git diff --check` 通过，指定 Python 3.10 最低回归 `86 passed`，扩展相关回归 `402 passed`，现已获授权线性合入并回读远端 trunk；当前 trunk 与候选同为 `7810f198...`。未部署、未重启 |
| DAV-808 / 2-3 | 表中明确是批次 2 的 2-3；DAV-824 规格草案已读完并刷新到当前主干 `7810f198...`，仍需定约束范围、守卫落点、命中处置、误报策略、角色范围，以及 V-03 global prompt 基线；且与 0-A 抢 `api/main.py`，部署后再开 | 保持 backlog；决策简报已备妥，未擅自开实施卡 |

## “政策”到底指什么

当前没有一个需要先确认的总施工政策。这里需要决策的只是 DAV-808 的六项产品契约：

1. 是否把 custom prompt 纳入 E-02 硬约束；
2. 守卫放在保存/解析冻结/研究经理组装中的哪一层（是否分层）；
3. 命中后的保存入口与运行时处置；
4. 否定句、普通“权重”和不确定文本的误报策略；
5. 先约束 `research_manager` 还是五个可注入角色；
6. V-03 是否纳入或禁用 global prompt hash `5489166b29c2`。

这六项只在进入批次 2-3 时需要决策，不能把它们当作 L4/L5 的前置条件；但按施工表，
DAV-808 在部署完成前也不能开实施卡。

## 仍然遵守的边界

- 不启动真实采集，不读写 Cookie，不写生产库。
- 当前不部署 `7810f198...`，不重启服务。
- 代码审查任务后续派给“代码审核员”（必要时“代码审核员2”），不再派给“独立代码审核员”。
- L5 候选完成后仍须同一完整 SHA 的代码审核、D-012 红队场景和 RT-FULL；审核通过不等于 WorkBuddy 放行合入。
