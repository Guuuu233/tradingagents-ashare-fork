# DAV-273 部署 8866494（5 行业产业链 + 两阶段分析拓扑）

主干已核验：`target/codex/dav-4-p2a-trunk@8866494f8cae9f648aff0c4c0a2f9f5cf680e8dd`
父链：`2e9e674 → b1fa020`（5 行业）`→ 8866494`（两阶段拓扑）
DAV-268 / DAV-271 独立终审均 PASS。

当前宿主 healthz 仍为 `2e9e674`，PID 约 35021，active reports=0。持久配置必须保持 3/1。

## 保护

禁止 `reset --hard` / `clean -fd`；禁止改 `.env`、providers、role_bindings、用户 Key、持久轮次。WIP 先保护再 FF。

## 部署

1. 再确认远端主干仍是 `8866494`。
2. `SELECT COUNT(*) FROM reports WHERE status IN ('pending','running')` = 0。
3. 宿主 fast-forward 到该 SHA。
4. kill 旧 PID，`lsof -iTCP:8000` 直到空。
5. 完整 `no_proxy` + `DATABASE_URL=sqlite:///./data/tradingagents.db` + `.venv310` 重启 uvicorn :8000。
6. 验收：新 PID、cwd=项目根、打开 `data/tradingagents.db`、healthz `commit_sha=8866494f8cae9f648aff0c4c0a2f9f5cf680e8dd`。

## 冒烟（只读，不改配置）

- 进程 import：`setup.py` 含 phase1/phase2 分流；`INDUSTRY_LINKAGE_MAP` 含半导体/石化/金融地产。
- `POST /v1/models/fetch` 正确账户 + `http://100.65.130.33:8317/v1` 仍可拉列表（回归 DAV-261）。
- 不发完整 3/3 真实分析（耗时长、配额紧）；本卡只证明代码已加载。

不得 @项目调度助手。立即执行。
