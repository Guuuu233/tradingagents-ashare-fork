# DAV-264 主干 fast-forward 合入 2e9e674（用户自有 URL 拉模型列表）

Hermes 代表用户授权合入。独立终审 DAV-262 PASS。

## 门闩

- 当前主干必须仍是：`target/codex/dav-4-p2a-trunk@0b10041f9e68b5d0116b76c36cd629acb365f10d`
- 候选：`2e9e674ceff062b7b35ae010a954b61f7575c47e`（`agent/work2/dav-261-models-fetch`）
- 父提交必须是 `0b10041`，ahead 1 / behind 0
- 仅 2 文件：`api/main.py`、`tests/test_models_fetch_ssrf.py`

## 操作

独立 checkout，禁止改宿主工作树、禁止 `reset --hard` / `clean -fd`、禁止碰 `.env`。
`git merge-base --is-ancestor 0b10041 2e9e674` 必须成立。
对 `codex/dav-4-p2a-trunk` **fast-forward only** 到 `2e9e674`。禁止 merge commit、禁止强推。
GitHub API 回读主干 HEAD = `2e9e674ceff062b7b35ae010a954b61f7575c47e`。

## 禁止

不部署、不重启 8000、不改用户模型/Key/providers。不得 @项目调度助手。立即执行。
