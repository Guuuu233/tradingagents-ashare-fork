## B1 交付后的核验顺序（三步）

**B1 范围到此为止，不再扩展。** 主控将按以下顺序逐条执行核验。

---

### 第一步：证明 S1 的真实路径

必须看到完整链路：

```
login → connect 被拒 → 清理 → 原异常穿透        且  未进入 send_msg
```

- **不接受**只断言「抛了异常」就算过——要能看出**中间每一环真的发生了**
- **send 次数必须为零**（对 `send` / `send_msg` 打桩计数，断言 `== 0`）

### 第二步：独立重置状态后再证明 S2

证明加固**在安装阶段终止**，**完全没有调用 `login`**（`call_count == 0`）。

「独立重置」是硬要求：S2 必须在**不受 S1 残留影响**的状态下断言。

### 第三步：检查两个测试是否共享会互相覆盖的模块状态

**这一步必须做**——本次根因本身就是模块级全局：

```
baostock.common.context.default_socket    模块级全局
SocketUtil.instance                        单例
新增加固的「是否已安装」标志               大概率也是模块级
```

若两个测试共享这些状态，**先跑的会覆盖/污染后跑的，断言可能假绿**。

**处置（二选一）：**

1. **拆进独立进程**（如 `subprocess` 跑单测）
2. **显式重置后分别断言**——fixture 里把全局与单例恢复到已知初值，**并在断言前确认初值成立**

> **不接受**靠用例书写顺序碰巧不冲突——那不是隔离，是巧合。

### 自检三问（沿用）

1. 把被测修复整个删掉，这两条测试会不会依然绿？
2. 是否用新建对象 / `asyncio.run()` 丢弃了被测状态宿主？
3. 是否用 `side_effect=` 注入了本该由真实路径产生的异常？

---

其余不变：谱系 `8589e65 → B1 → B2` 线性；不 amend 三个既有提交；累计白名单按 `git diff --name-only 8589e65..<A+B SHA>` 核；禁 `setdefaulttimeout`；禁全局 session 锁；禁裸 `except` 与 `except Exception: pass`。

**第二阶段不构成合入授权**；主线 `5a0320f` 继续 HOLD。
