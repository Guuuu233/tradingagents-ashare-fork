## 603a92e 系统层离线隔离补证 v2 —— 判定（采样器三处缺陷已修）

同 SHA `603a92e`、零代码改动、无新 SHA。采样器按指正修正后重跑。

---

### 一、采样器修正（对照上轮三处缺陷）

| 上轮缺陷 | v2 修正 |
|---|---|
| 采样晚于 pytest ~8s | **采样器先启动轮询**，14:27:17 就位 → 14:27:20 检测到 pytest root，**零启动间隙** |
| 只查两层后代 | **每 tick 重建 `ps -eo pid,ppid` 全表，自 root 任意深度闭包遍历** |
| 漏记短命子进程 | **SEEN_PIDS 集合记录所有出现过的 PID**（含已退出者） |
| 只查 TCP ESTABLISHED | **TCP 全状态 + UDP**，均排除 loopback |

### 二、v2 采样结果

```
采样点数            193（14:27:20 → 14:34:03，覆盖全量全程）
被捕后代 PID 总数    27 个（v1 只看到 2 层，v2 递归抓到全部短命子进程）
                    302 336 371 412 447 669 92623 93724 93819 94216 94249
                    94282 94316 94353 94384 94417 94452 94680 95450 95867
                    95902 95940 95979 98135 98553 99128 99161
pytest 树非本地连接  0 条（TCP 全状态 + UDP，EXT-PID 记录 0）
pytest_ext>0 采样点   0 个
环境基线            32–61 条波动 → 采样器工作正常
```

### 三、全量结果（同前，无新增回归）

```
20 failed, 4851 passed, 1 skipped, 4 deselected in 398.63s · 自然结束
20 个失败文件与基线 5a0320f 逐项完全一致
```

### 四、deny 台账（更正计数口径）

```
主进程台账 rt_deny_20260917_142721_92623_bae682.log
原始记录            2359
人工正控            1 条（NODEID:SENTINEL_DENY，PHASE:sentinel）
实际非正控拒绝       2358 条  ← 你的口径正确
```

**更正说明**：此前我 `grep -c sentinel` 得 5 是把测试名含 "sentinel" 的**业务用例**（`test_social_analyst_..._sentinel` 等 4 条）误计为正控；按 `PHASE:sentinel`/`NODEID:SENTINEL` 严格计，正控仅 1 条。

分类明细（含正控）：`getaddrinfo` 2010 / `connect` 211 / `curl_cffi` 136 / `sendto` 1 / `gethostbyname` 1 = 2359 原始数。

### 五、证据指纹

| 证据 | 文件 | SHA256 前 32 |
|---|---|---|
| 全量日志 | `/tmp/dav1009_rtfull_iso_603a92e.log` | `9aa25c79a8932e48ff99f7f7d5c0ec60` |
| **v2 系统采样** | `/tmp/dav1009_isolation_lsof_v2.log` | `dd1fb31251e926a9d5d897ade667f220` |
| deny 台账 | `rt_deny_..._92623_bae682.log` | 见上 |
| observe 台账 | `rt_observe_..._92623_bae682.log` | 见上 |

### 六、判定（边界化措辞）

```
代码回归门禁           PASS（20 失败全为基线，零新增）
完整回归门禁           PASS（自然结束）
Python 层网络拒绝门禁   PASS（2358 条非正控拒绝）
系统层离线隔离门禁      PASS —— 持续采样期间（193 点 × 2s，零启动间隙，
                       任意深度 27 后代）未观察到 pytest 树的成功非本地连接
```

**边界**：结论为「采样期间未观察到」，非绝对零外连证明；TCP 全状态 + UDP 已覆盖，2s 间隔理论上仍可漏 <2s 瞬态连接，但护栏已在 Python 层 deny 全部 2358 次尝试。

### 七、仍受约束

- 不构成合入/部署授权 · 主线 `5a0320f` HOLD · 生产库/服务未动
- Phase 3 / 卡 C / DAV-998 未启动
