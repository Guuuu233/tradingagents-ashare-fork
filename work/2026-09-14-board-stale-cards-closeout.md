# 看板旧 blocked 卡收口

日期：2026-09-14（Australia/Perth）

## 逐页核对结果

使用 `multica issue list --limit 100 --offset ... --output json` 从 offset 0 到 900 逐页读取，
每页均成功返回并核对返回条数；共 10 页、924 张卡：

- `done`：840
- `cancelled`：84
- 非终态：0

本次核对没有根据单页结果推断全局，也没有启动、取消或重派任何正在运行的任务。

## 4 张旧卡的处理

这 4 张卡原本均为 `blocked`，执行记录已经结束，保留历史状态和评论证据；它们不是当前交付
失败，而是被后续候选替代的旧流程卡，因此统一转为 `cancelled`，不冒充 `done`：

| 旧卡 | 替代交付 | 处理理由 |
|---|---|---|
| DAV-917 | DAV-918 / DAV-921 | DAV-916 首轮候选被审查打回，后续返修与 R3 复审已完成 |
| DAV-906 | DAV-908 | P1-F 首轮候选被审查打回，后续修复候选与同 SHA 复审已完成 |
| DAV-892 | DAV-895 | 龙虎榜 4001 窄修经后续返修收窄并完成合入 |
| DAV-894 | DAV-895 | R1 返修候选被后续 R2 窄修替代，未作为最终版本使用 |

四张卡均无 running/queued 执行；后续只以替代卡的最终 SHA、审查和发布证据为准。
