## 台账口径修正（覆盖此前不准确表述）

**只修正口径，未启动新测试/新 run，未合入/部署。**

---

### 一、两轮并发旁证（非两轮独立运行）

```
轮次1  193 采样点 · 27 PID（1 pytest 根 + 26 后代）· 398.63s · 14:27:20–14:34:03
轮次2  198 采样点 · 30 PID（1 根 + 29 后代）· 406.92s · 14:28:14–14:35:01
```

**两轮运行时间大量重叠，属并发旁证，不能称为"两轮独立运行"，采样点不得相加作为更长覆盖。**

每轮各自生成全新 deny/observe 日志，**PID、时间戳、SHA256 均不同，非复用 v1 台账**：

| 台账 | PID | 时间窗 | SHA256 前 32 |
|---|---|---|---|
| deny 轮1 | 92623 | 14:27:21–14:34:01 | `5615f6b88d4894a6472629ea39972181` |
| deny 轮2 | 93567 | 14:28:14–14:35:01 | `509445b2f03b0b2fa46b23ba243eea12` |

**每轮各自：2359 原始 − 1 条 PHASE:sentinel 正控 = 2358 条非正控拒绝**（不是合并 4718）。

### 二、措辞收窄

- "零启动间隙" → 收窄为：**采样器提前就位，首次采样与 pytest 启动发生在同一秒**；仍保留 2s 间隔可能漏瞬态行为的边界
- SEEN_PIDS → 收窄为：**只证明采样时刻仍存在并被发现的 PID**，不宣称绝对捕获所有 <2s 生命周期进程
- "未观察到成功非本地连接" → 保持边界化，非绝对零外连

### 三、权威门禁状态

```
代码回归门禁           PASS
完整回归门禁           PASS
Python 层网络拒绝门禁   PASS（每轮各 2358 条非正控拒绝）
系统层离线隔离门禁      PASS（仅限"采样期间未观察到"的边界化结论）
合入授权              暂无
部署授权              暂无
```

### 四、整合基线口径

`4fa7681` 只是本地另一 agent 分支的检出位置，**不作整合基线**。受控整合基线以远端为准：

```
origin/codex/dav-4-p2a-trunk = 5a0320f0618d203e95e7197e08b110c7850078d4（已实测回读）
origin/agent/1/a563fbe02617-b2 = 603a92e（候选）
```

### 五、仍冻结

不合入 / 不部署 / 不动主线、生产库、服务 / Phase 3、卡 C、DAV-998 均未启动。
