# DAV-887 前端静态验收（2026-09-14）

## 范围

- 代码内容来自 `63d5648bca7c49f57e1211d848cbc1d02ff6b3a5`；验证工作在其文档-only 后代 `2e6cced6cfe1dae598ad332afc703a30cfda4147` 的隔离 worktree 完成，前端源码未改变。
- 依赖使用 `npm ci --ignore-scripts` 安装在临时 worktree 的 `node_modules`，未修改 lockfile。

## 结果

- `npm test -- --run`：15 个测试文件、144 个测试全部通过。
- `npm run build`：TypeScript 检查和 Vite 生产构建成功，生成 `dist`；仅有既有的 Vite `__dirname` 配置迁移提示和 bundle 超过 500 kB 的性能提示，无构建失败。
- 临时 Vite 服务入口 HTTP smoke：`GET http://127.0.0.1:5173/` 返回 HTTP 200，HTML 含 `#root`、`/src/main.tsx` 和中文 dashboard 标题；服务随后已停止。

## 边界

这证明 DAV-887 的源码、组件测试、生产构建和临时入口可服务。它不证明实际部署平台已经发布了该 bundle，也不替代生产浏览器中的 TrackingBoard/Portfolio 视觉 smoke；后者仍是独立收口项。
