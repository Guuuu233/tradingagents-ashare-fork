# DAV-1055 同 SHA 复审记录

日期：2026-09-18

## 对象

- 候选：`061f007ebf58692024c78772cb21e1429695a569`
- 直接父：`9100a6070d0bcb32b8d16467fd9ac2811e4dc648`
- 远端候选分支：`origin/agent/2/c6da291c2b2f`
- 审查执行者：代码审核员（不是独立代码审核员）
- 审查卡：DAV-1055，结果：PASS，已完成

## 复审结论

日期入库的两条路径都已改为：先对未经 `[:10]` 截断的原始字符串做严格 `YYYY-MM-DD` 格式与真实日历校验，校验通过后才保存 canonical 日期。现场验证带 `T`、空格、时区后缀的 `trade_dates` 与 `bars[].date` 均被拒绝，合法日期仍可完成测量。

未知 ST 的 typed-missing、显式 `st:false`/`st:true`、`st_dates`、T+1/T+5、forward OOS 上界和 PIT 语义均复核通过。恢复旧的 `[:10]` 顺序后，8 个新增测试全部失败，证明新测试锁住了本轮缺陷。

## 结构与回归证据

- 远端 SHA 与候选逐位一致。
- 候选相对直接父仅改：`tradingagents/eval/v03_return_measure.py`、`tests/test_v03_offline_guard.py`。
- `git diff --check` 干净。
- 候选 RT-FULL：`4971 passed / 1 failed / 1 skipped / 6 deselected`。
- 直接父 RT-FULL：`4964 passed / 0 failed / 1 skipped / 6 deselected`。
- 候选唯一失败为 `test_provider_resource_cleanup.py::test_route_timeout_falls_back_to_next_provider`；该测试使用 10ms 真实超时，候选 SHA 单独重跑 6 次通过，审查员将其判为时序敏感 flake，未归因于本次两文件改动。
- 审查运行中曾误带未安装的 `pytest --timeout` 参数，随后已去掉并重新执行；该参数错误不计入代码结果。

## 合入记录

候选从当前主线 `6ee148699339efefc2f7f7548eb286be485524e1` 可快进，已推送到 `origin/codex/dav-4-p2a-trunk`。当前主线：`061f007ebf58692024c78772cb21e1429695a569`。

本次只合入代码，未部署、未重启服务，未写生产库。
