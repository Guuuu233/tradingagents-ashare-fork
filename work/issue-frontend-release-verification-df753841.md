# df753841 前端静态包发布核验

## 已知现象

- 运行中的 API 服务为 `df7538413ba7bb55593b1757feaf90b0bd514d1c`。
- 8000 的 `/healthz` 正常，但 `/` 返回 404。
- `api/main.py` 仅在当前工作目录下存在 `frontend/dist` 时挂载前端；当前 release copy 未发现该目录。

## 任务范围（只读/隔离）

1. 核实项目设计是由 8000 同时托管前端，还是另有独立前端服务/反向代理；不得凭 404 猜测。
2. 在 `df753841` 的干净隔离副本中检查 `frontend` 锁定依赖、`npm run build` 可行性和构建产物，不改源码。
3. 若依赖缺失或需要联网安装，记录阻塞，不擅自安装未知版本、不修改 lockfile。
4. 若能构建，只把产物放在隔离目录并报告，不复制到 `/private/tmp/ta-serve-df753841`，不重启 8000。
5. 核对前端来源、commit、构建时间、bundle 是否包含当前 df753841 相关 UI 契约；同时保持 API 404 保护语义（`/api/*`、`/v1/*` 不应被 SPA fallback 吞掉）。

## 交付

报告必须给出：服务入口证据、release copy 是否有 dist、node/npm 版本、构建命令和精确结果、产物路径/哈希、是否触网/安装、下一步是否需要单独部署授权。不得修改代码、数据库、配置、运行服务或主线。
