# DAV-922 / P2-55b 受控发布收口

日期：2026-09-14（Australia/Perth）

## 发布对象

- 线上发布完整 SHA：`f094d6a78bc699fc6224e57164d38455c2ad55a9`
- 该版本的代码变更来自 DAV-922 最终候选
  `c1ce3ab31ac22de1c28931f94fa2e98b0fa2e699`；`025427b...` 之后只有 RT-FULL 证据、
  PROJECT_STATE 和决定台账的治理文档提交，没有代码变更。
- 发布副本：`/private/tmp/ta-release-p2-55b-f094d6a-20260914`
- 发布前已核对目标远端为 `f094d6a...`，发布副本 clean，候选代码的复审、159 项关联回归
  和同口径全量对照均已完成。

## 发布前保护

- 生产 SQLite：`/Users/davidliu/Documents/TradingAgents-AShare/data/tradingagents.db`
- 发布前后大小均为 `285339648` bytes；SHA-256 均为
  `94d2f6740db4f2065100479dd5cb3ccf5d8a504447a55fa8f19635927ce83010`。
- 发布前备份：`work/tradingagents.db.bak-20260914-predeploy-f094d6a`；备份
  `quick_check=ok`、`integrity_check=ok`，reports 为 `1409/793/616`；备份 SHA-256 同为
  `94d2f6740db4f2065100479dd5cb3ccf5d8a504447a55fa8f19635927ce83010`。
- 发布进程使用已存在的 `.env` 与 `.env.local` 安全配置，并显式指定生产库绝对路径
  `DATABASE_URL=sqlite:////Users/davidliu/Documents/TradingAgents-AShare/data/tradingagents.db`；
  没有打印或更换任何密钥。

## 预启动与切换

- 第一次 8001 预启动因新 worktree 未继承 `TA_APP_SECRET_KEY` 被安全启动闸拒绝；没有使用默认
  密钥绕过。
- 继承安全配置后，第二次预启动发现新 worktree 没有旧发布副本中的 `data` 软链接，应用拒绝
  打开相对路径数据库。改用上面的已核对生产库绝对路径后启动成功；没有复制、迁移或写入
  数据库。
- 8001 预启动 `/healthz` 精确返回 `f094d6a78bc699fc6224e57164d38455c2ad55a9`；随后
  provider health 正常、600519.SH 两日只读行情返回 2 根 K 线、social status 为
  `disabled`，Dashboard 和新 bundle 返回 HTTP 200，未知 `/v1/` API 返回 404。
- 前端在发布副本重建：17 个测试文件/171 个测试通过；`npm run build` 成功。实际 bundle
  `/assets/index-BeTBfWjf.js` 返回 HTTP 200，SHA-256 为
  `e10e9f01a8d47356ac250a8a4ec3d50187969cd8f302d53a4a00dc2b2ce83ff5`。
- 旧服务 PID `42225` 在 8001 预启动通过后优雅停止；新服务 PID `59324` 在 8000 运行，
  工作目录为 `/private/tmp/ta-release-p2-55b-f094d6a-20260914`，父进程 `71186`。

## 发布后回读

- 8000 `/healthz` HTTP 200，`commit_sha` 和 `build_identity` 均精确为
  `f094d6a78bc699fc6224e57164d38455c2ad55a9`，`executor_queued=0`，
  `executor_threads=1`。
- 8000 provider health 为 `ok`；只读行情返回 2 根 K 线；social 仍为 disabled；Dashboard
  和 bundle HTTP 200；未知 `/v1/` API 为 404。
- 发布后生产 SQLite 仍为 `285339648` bytes、SHA-256
  `94d2f6740db4f2065100479dd5cb3ccf5d8a504447a55fa8f19635927ce83010`，
  `quick_check=ok`、`integrity_check=ok`，reports 仍为 `1409/793/616`。
- 8001 预启动进程已关闭；旧发布副本 `/private/tmp/ta-release-main-79757a6-20260914`
  保留作为回退核验点。

## 边界

本次只发布已经复审、全量对照并合入的代码；没有启动真实分析或真实模型 warmup，没有写入
新报告，没有真实社交采集、Cookie、信用加权、凭据轮换或历史重写。provider/行情请求是
只读烟测，不是收益实验或生产业务链路证据。P2-55b 的真实 warmup 和生产报告字段/trace/
readback 证据仍未取得。
